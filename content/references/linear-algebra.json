[
  {
    "id": "linear-algebra-vectors-1",
    "kind": "term",
    "name": "Coordinate vector",
    "aliases": ["Coordinate vector"],
    "linkAliases": [],
    "quick": "An ordered list of real components, interpreted as data or a displacement.",
    "definition": "A real [coordinate vector](ref:linear-algebra-vectors-1) is an ordered list of real numbers. Its entries are called components. The notation $\\mathbb{R}^n$ means the set of all lists with exactly n real components; read it as “R to the n,” or “real n-dimensional coordinate space.” Here n is a positive integer, the dimension of this coordinate space. The superscript specifies the number of coordinates rather than a numerical power of a set.\n\nFor example, $u=(3,2)$ is a vector with two components. As a displacement, it means three units right and two units up. As data, it could mean three hours spent on one activity and two on another. The arithmetic is the same, but units and interpretation come from the application. Adding measurements with incompatible meanings can be mathematically possible and scientifically meaningless.\n\nA vector is not tied to a particular starting point. An arrow from the origin to (3,2) and an arrow from (1,1) to (4,3) represent the same displacement. A position is a point; its position vector is the displacement from the chosen origin. Keeping those ideas distinct helps when we later distinguish linear transformations from translations.\n\nWe can write the same components vertically as a column. Parentheses and square brackets are both common; a vertical arrangement does not change the [component](ref:la-term-component) values. Unless a lesson says otherwise, our matrix calculations use column vectors. In ordinary prose, coordinate tuples remain convenient shorthand.\n\n### Coordinates carry meaning\n\nSuppose a weather record is (12,5,-2), in the order temperature, wind speed, and overnight change in temperature. It has three components, but those components do not describe three spatial directions. A negative third component records a decrease; it does not invalidate the vector. Before doing arithmetic, write down the coordinate order and units. If another record uses a different order, reorder it before comparing corresponding components.\n\nFor a displacement, subtract start from finish in every coordinate. An arrow from (4,-2,1) to (1,3,1) has displacement (-3,5,0). Its zero third component means no change in that coordinate, not that the arrow is the zero vector. If only the starting point moves while the endpoint stays fixed, the displacement changes. It stays the same only when both endpoints are translated by the same amount.",
    "example": "An arrow starts at (2,-1) and ends at (5,3). Give its displacement vector. Explain whether translating both endpoints by the same amount changes that vector, and whether moving only the starting point would do so.\n\nThe displacement is (3,4): subtract the starting coordinates from the ending coordinates. Translating both endpoints by the same amount preserves these differences. Moving only the starting point while fixing the endpoint changes the displacement.",
    "confusion": "A vector's component order matters. Translating an arrow without changing its displacement does not create a different vector.",
    "lesson": "linear-algebra-vectors",
    "section": "Coordinates and vectors",
    "related": ["linear-algebra-vectors-2"]
  },
  {
    "id": "linear-algebra-vectors-2",
    "kind": "term",
    "name": "Vector addition and scaling",
    "aliases": ["Vector addition and scaling"],
    "linkAliases": [],
    "quick": "Add corresponding components; multiply every component by a scalar.",
    "definition": "Vectors of the same size add component by component. For $u=(3,2)$ and $v=(-1,1)$, we obtain $u+v=(2,3)$. Geometrically, follow the displacement u and then the displacement v. The final displacement from the original starting point is their sum. Moving the second arrow so that its tail meets the first arrow's head does not change that second displacement.\n\nA [scalar](ref:la-term-scalar) is an ordinary number used to multiply a vector. [Scalar multiplication](ref:linear-algebra-vectors-2) multiplies every component: $2u=(6,4)$. For a nonzero vector, a positive scalar stretches or shrinks its arrow while preserving its direction; a negative scalar reverses it as well. Multiplication by zero produces the zero vector, whose components are all zero and which has no direction.\n\nSubtraction means adding the negative vector. Thus $u-v=(4,1)$ in this example. The difference is also the displacement from the point represented by v to the point represented by u. Notice the order: reversing the subtraction reverses the arrow.\n\n\n\n### A calculation you can check backward\n\nTake u=(2,-1,3) and v=(-4,2,1). First scale each entry: three u is (6,-3,9), while negative two v is (8,-4,-2). Their sum is (14,-7,7). To check the result, subtract three u and recover negative two v. This backward check catches the common mistake of applying a minus sign only to the first component.\n\nVector addition is commutative and associative because each coordinate uses ordinary real-number addition. Scaling distributes over addition for the same reason: the ith component of c(u+v) is c times the sum of the two ith components, which equals the sum of their separate products with c. This is an argument for arbitrary vectors, rather than a conclusion based on several successful examples. You may regroup a long sum, but you may not add vectors with different numbers of components.",
    "example": "For u=(2,-3) and v=(-1,4), find u+v, u-v, and -2u. Explain the geometric effect of the negative scalar.\n\nThe sum is (1,1), the difference is (3,-7), and -2u=(-4,6). A negative factor reverses direction; its magnitude two doubles the length.",
    "confusion": "A scalar is a number, while the result of scaling a vector is a vector. Negative scaling reverses direction.",
    "lesson": "linear-algebra-vectors",
    "section": "Adding and scaling",
    "related": ["linear-algebra-vectors-1"]
  },
  {
    "id": "linear-algebra-vectors-3",
    "kind": "term",
    "name": "Linear combination",
    "aliases": ["Linear combination"],
    "linkAliases": [],
    "quick": "A sum of scalar multiples of specified vectors.",
    "definition": "A [linear combination](ref:linear-algebra-vectors-3) is a sum of scalar multiples of given vectors. For example, $2u+3v=(3,7)$ with the vectors above. The numbers 2 and 3 are coefficients. The word linear here describes the allowed construction: scale each supplied vector, then add. We are not multiplying components together or applying squares to them.\n\nThe standard coordinate vectors in the plane are $e_1=(1,0)$ and $e_2=(0,1)$. Every vector $(a,b)$ can be written as $ae_1+be_2$. Read this as “a times e sub one plus b times e sub two.” The scalars a and b are arbitrary real numbers. The subscripts distinguish two named vectors; they are not extra coordinates.\n\nOther building blocks can work too. Let p=(1,1) and q=(1,-1). To build (4,2), use three copies of p and one copy of q: the horizontal components add to four while the vertical components add to two. If we instead have only p and 2p, every combination still lies on the same diagonal line. Having more listed vectors does not necessarily provide more freedom. Later lessons will make that observation precise.\n\n### Working backward to find coefficients\n\nTo express (7,1) using p=(1,1) and q=(2,-1), call the coefficients a and b. Matching first components gives a+2b=7; matching second components gives a-b=1. The second equation says a=1+b. Substitution into the first gives 1+3b=7, so b=2 and a=3. Check by rebuilding: three copies of p plus two copies of q give (7,1).\n\nThis uses familiar simultaneous equations, not a new rule for guessing coefficients. Sometimes no coefficients work, as with two diagonal building blocks and a target off their line. Sometimes many lists work: with p and 2p, the vector 4p can be made with coefficients (4,0), (2,1), or (0,2). Do not confuse the number of supplied vectors with the number of genuinely different directions.\n\n\n$\n3\\begin{pmatrix}1\\\\1\\end{pmatrix}+2\\begin{pmatrix}2\\\\-1\\end{pmatrix}=\\begin{pmatrix}7\\\\1\\end{pmatrix}\n$",
    "example": "Let u=(1,2) and v=(2,-1). Find 3u-2v, showing the component arithmetic.\n\nThree u is (3,6), and negative two v is (-4,2). Adding gives (-1,8). Both components must be scaled before the vectors are added.",
    "confusion": "More supplied vectors need not mean more reachable directions. Coefficients can be negative unless the application restricts them.",
    "lesson": "linear-algebra-vectors",
    "section": "Building linear combinations",
    "related": ["linear-algebra-vectors-2"]
  },
  {
    "id": "linear-algebra-vectors-4",
    "kind": "term",
    "name": "Linear resource model",
    "aliases": ["Linear resource model"],
    "linkAliases": [],
    "quick": "A model that adds proportional contributions from each activity.",
    "definition": "Suppose a small workshop makes two products. One unit of the first uses (2,1) units of two resources; one unit of the second uses (1,3). Making three units of the first and two of the second uses (8,9), obtained by scaling each resource vector by the production amount and adding.\n\nThis model assumes [resource use](ref:linear-algebra-vectors-4) is additive and proportional. It ignores setup costs, waste that changes with volume, and bulk discounts. A vector calculation does not establish that these assumptions are appropriate; it tells us what follows if they are. In applications, interpret the components and check the assumptions before trusting the result.\n\nThere is also a difference between a mathematical combination and a feasible production plan. Linear algebra permits negative real coefficients. A workshop usually requires nonnegative whole-number quantities. Constraints such as these belong to the model, even though the underlying vector operations remain the same.\n\nFor practice, show component arithmetic and briefly identify what the result represents. You can write vectors as tuples initially. Optional typing help also shows columns, using a matrix environment: ampersands separate columns and two backslashes separate rows. The preview should preserve the order of your components.\n\n### Testing the model rather than only the arithmetic\n\nIf each batch uses (2,3) units of water and power, four batches use (8,12) under a proportional model. If starting the machine also consumes (1,5), the total is instead (9,17). The fixed startup cost is paid once, so doubling the number of batches does not double the whole total. A correct calculation with the wrong assumptions is still the wrong model.\n\nSimilarly, an average of two measurement vectors is obtained by adding them and scaling by one half, provided the measurements use matching units and order. That average need not be a feasible individual observation: the average number of children in a household may be fractional. Separate what the arithmetic represents from what an individual object is allowed to be.",
    "example": "A product uses (3,2) units of wood and metal, in that order. A second product uses (1,4). Find the resources for two of the first and three of the second, and name one assumption of this calculation.\n\nThe resource vector is two times (3,2) plus three times (1,4), or (9,16). The model assumes additive resource use proportional to production, with no extra setup use or changing waste rate.",
    "confusion": "A valid vector calculation does not establish physical assumptions such as proportional use or feasible production counts.",
    "lesson": "linear-algebra-vectors",
    "section": "Linear models and their limits",
    "related": ["linear-algebra-vectors-3"]
  },
  {
    "id": "linear-algebra-dot-products-1",
    "kind": "term",
    "name": "Dot product",
    "aliases": ["Dot product"],
    "linkAliases": [],
    "quick": "Multiply corresponding real components and add, producing one scalar.",
    "definition": "For real vectors of the same size, the [dot product](ref:linear-algebra-dot-products-1) multiplies corresponding components and adds the products. In two dimensions, $u\\cdot v=u_1v_1+u_2v_2$. Here u and v are vectors, while u sub one, u sub two, and the corresponding v symbols are their real components. The centered dot means this operation, not a new vector component.\n\nFor u=(2,-1) and v=(3,4), the dot product is two times three plus negative one times four, which is 2. The result is a scalar. This is different from multiplying two vectors component by component and keeping a list of products; that operation exists in other contexts but is not the dot product.\n\nThe same rule works with any finite number of components. Because ordinary multiplication commutes, exchanging the two vectors leaves their dot product unchanged. It also distributes across vector addition. These facts will let us expand squared distances without drawing every example.\n\n### Why distribution is useful\n\nFor u=(1,-2,2), the dot product with itself is 1+4+4=9. Every square is nonnegative, so a real vector has zero dot product with itself only when all its components vanish. This observation will explain why squared length detects zero.\n\nTo expand the dot product of u+v with u-v, distribute in both positions. The four terms are u dot u, negative u dot v, v dot u, and negative v dot v. The middle terms cancel by symmetry. The result is the squared length of u minus the squared length of v. Explaining that cancellation is more useful than memorizing a separate vector identity.",
    "example": "Compute the dot product of (1,-2,3) and (4,0,-1). Is the result a scalar or a vector?\n\nThe component products are 4, 0, and -3; their sum is 1. The result is a scalar, not a list of three products.",
    "confusion": "This is not a list of componentwise products. A zero dot product does not mean either input is zero.",
    "lesson": "linear-algebra-dot-products",
    "section": "A scalar from two vectors",
    "related": ["linear-algebra-dot-products-2"]
  },
  {
    "id": "linear-algebra-dot-products-2",
    "kind": "term",
    "name": "Euclidean norm",
    "aliases": ["Euclidean norm"],
    "linkAliases": [],
    "quick": "The square root of the sum of squared components: a vector's usual length.",
    "definition": "The [Euclidean norm](ref:linear-algebra-dot-products-2) is the usual geometric length of a vector. We write $\\|u\\|=\\sqrt{u\\cdot u}$, read “the norm of u equals the square root of u dot u.” The double vertical bars mean length here. Squaring and adding the components gives the familiar Pythagorean calculation, extended to more coordinates.\n\nFor u=(3,4), the squared length is 25 and the length is 5. Length is nonnegative, and it is zero only for the zero vector. Distance between two points is the norm of their difference, because that difference is the displacement connecting them.\n\nA [unit vector](ref:la-term-unit-vector) has length one. Normalizing a nonzero vector means dividing it by its norm. Thus (3/5,4/5) is a unit vector pointing in the direction of (3,4). Division here scales every component by the reciprocal of the length. The zero vector cannot be normalized: division by its zero length is undefined, and there is no direction to preserve.\n\n### Normalization changes size, not direction\n\nFor v=(-2,1,2), the sum of squared components is 9, so its length is 3 and its [normalization](ref:la-term-normalization) is (-2/3,1/3,2/3). The negative coordinate remains negative. A unit vector is not a vector with every entry equal to one; in fact (1,1,1) has length square root of three.\n\n$\n\\frac{1}{3}\\begin{pmatrix}-2\\\\1\\\\2\\end{pmatrix}=\\begin{pmatrix}-2/3\\\\1/3\\\\2/3\\end{pmatrix}\n$\n\nScaling a vector by a real number c multiplies its squared length by c squared, and therefore multiplies its length by the absolute value of c. The absolute value matters when c is negative. Likewise the distance between points depends on the difference vector, so translating both points by the same displacement does not change their distance.",
    "example": "Find the norm of (6,8) and a unit vector in its direction. Explain why your normalized vector has length one.\n\nThe norm is the square root of 36+64, or 10. Dividing gives (3/5,4/5). The sum of its squared components is 9/25+16/25=1, so its norm is one.",
    "confusion": "Norm is nonnegative. The zero vector has no direction and cannot be normalized.",
    "lesson": "linear-algebra-dot-products",
    "section": "Length and unit vectors",
    "related": ["linear-algebra-dot-products-1"]
  },
  {
    "id": "linear-algebra-dot-products-3",
    "kind": "term",
    "name": "Orthogonal vectors",
    "aliases": ["Orthogonal vectors"],
    "linkAliases": [],
    "quick": "Real vectors whose dot product is zero; nonzero examples are perpendicular.",
    "definition": "For nonzero vectors with angle theta between them, $u\\cdot v=\\|u\\|\\|v\\|\\cos\\theta$. This identity connects the component calculation to geometry; it follows from the cosine rule applied to the triangle with sides u, v, and their difference. Theta is an angle between zero and pi radians, or between zero and 180 degrees.\n\nFor angles strictly between zero and 180 degrees, a positive dot product corresponds to an acute angle, zero to a right angle, and a negative value to an obtuse angle. The endpoint cases are parallel vectors pointing in the same or opposite direction. We call vectors [orthogonal](ref:linear-algebra-dot-products-3) when their dot product is zero. For nonzero vectors this means perpendicular. Algebraically the zero vector is orthogonal to every vector, although an angle involving it is not defined.\n\nFor example, (1,2) and (2,-1) are orthogonal because the two component products cancel. Neither vector needs to lie along a coordinate axis. Orthogonality depends on the relation between the vectors, not on their orientation on the page.\n\n\n\n### Reading the sign carefully\n\nThe angle classification needs one boundary correction: a positive dot product can also mean angle zero, and a negative dot product can also mean angle 180 degrees. Strictly acute means between zero and 90 degrees; strictly obtuse means between 90 and 180 degrees. Parallel vectors pointing the same way have cosine one, while opposite directions have cosine negative one.\n\nFor u=(1,0) and v=(1,1), the dot product is one and the product of lengths is square root of two. Their cosine is one divided by square root of two, hence their angle is 45 degrees. For perpendicular vectors, the squared length of their sum is the sum of their squared lengths: distribute the dot product of the sum with itself and use the zero cross terms. This is the Pythagorean theorem expressed without choosing coordinate axes.",
    "example": "Determine whether u=(2,3) and v=(3,-2) are orthogonal. Does their orthogonality imply they are unit vectors?\n\nTheir dot product is 6-6=0, so they are orthogonal. Each has norm the square root of 13, not one. Perpendicularity and unit length are separate conditions.",
    "confusion": "Orthogonality does not imply unit length. An angle involving the zero vector is undefined even though its dot products are zero.",
    "lesson": "linear-algebra-dot-products",
    "section": "Angle and orthogonality",
    "related": ["linear-algebra-dot-products-2"]
  },
  {
    "id": "linear-algebra-dot-products-4",
    "kind": "term",
    "name": "Cosine similarity",
    "aliases": ["Cosine similarity"],
    "linkAliases": [],
    "quick": "For nonzero vectors, dot product divided by the product of their norms.",
    "definition": "A dot product also computes a weighted total. If prices are (2,5,3) and quantities are (4,1,2), their dot product is the total cost, 19. This reading does not require an angle, but it does require pairing the same product in the same coordinate position.\n\nComparing the dot product alone can confuse alignment with size: doubling one vector doubles the result even though its direction is unchanged. For two nonzero vectors, dividing their dot product by the product of their norms removes that scale effect and gives the cosine of their angle. This is called [cosine similarity](ref:linear-algebra-dot-products-4). Its values lie between negative one and one. A value of one means the vectors point in the same direction, not that their original measurements were identical.\n\nThere are practical limitations. Rescaling one feature can change angles in a dataset, and features measured in different units may need a carefully chosen scaling. Linear algebra explains the calculation; interpreting similarity still requires judgment about the data.\n\nIn your answers, distinguish a dot product, a norm, and a vector. They have different meanings even when some of their numerical values happen to coincide. Use an exact square root when a decimal approximation would hide a useful relationship.\n\n### Similarity depends on the representation\n\nTwo records (1,2) and (2,4) have the same direction. A third record (2,1) has the same two numbers as the first in reverse order, but a different direction. Cosine similarity removes overall magnitude, not coordinate meaning. It is undefined if either record is zero.\n\nIf you convert just one feature from metres to centimetres, that coordinate becomes a hundred times as large. Angles between records can change even though the underlying objects did not. When choosing a similarity measure, ask which changes you want to ignore. There is no universal instruction to normalize everything: sometimes magnitude, such as total sales volume, is precisely the information you need.",
    "example": "Prices are (3,2,5) and quantities are (2,4,1), paired in the same order. Compute the total cost and explain the role of the dot product.\n\nThe cost is 3 times 2 plus 2 times 4 plus 5 times 1, or 19. The dot product pairs each price with its own quantity and adds those item costs.",
    "confusion": "One means matching directions, not equal vectors. Feature units and scaling influence the interpretation.",
    "lesson": "linear-algebra-dot-products",
    "section": "Weighted totals and similarity",
    "related": ["linear-algebra-dot-products-3"]
  },
  {
    "id": "linear-algebra-matrices-1",
    "kind": "term",
    "name": "Matrix and transpose",
    "aliases": ["Matrix and transpose"],
    "linkAliases": [],
    "quick": "A rectangular array; transposing exchanges its rows and columns.",
    "definition": "An m by n matrix has m rows and n columns, where m and n are positive integers. An entry is one number in the array. We write $A_{ij}$ for the entry in row i and column j of matrix A, numbering rows and columns from one. The first subscript is always the row in this convention.\n\nFor example, $A=\\begin{pmatrix}1&2\\\\3&4\\end{pmatrix}$ has two rows and two columns, and its entry in row two, column one is 3. A square matrix has the same number of rows and columns. A column vector is an n by one matrix when we use matrix notation.\n\nThe [transpose](ref:la-term-transpose) exchanges rows and columns. The notation $A^T$ is read “A transpose”; T is a label for this operation, not an exponent. The example's transpose has rows (1,3) and (2,4). Transposing an m by n matrix produces an n by m matrix. Adding matrices requires equal shapes and adds corresponding entries; scalar multiplication scales every entry.\n\n### Rectangular arrays are ordinary matrices\n\nConsider a table with rows (1,2,0) and (-1,3,4). It has two rows and three columns, so it can multiply a three-component input. Its transpose has rows (1,-1), (2,3), and (0,4). Transposing twice restores the original table. A row and a column can contain the same entries but have different shapes, which changes what they can multiply.\n\nA matrix is symmetric when it equals its transpose. This requires a square shape and matching entries across the main diagonal. For example, rows (2,-1),(-1,5) give a [symmetric matrix](ref:la-term-symmetric-matrix); a square matrix with rows (2,0),(-1,5) is not symmetric.",
    "example": "A matrix has rows (1,2,3) and (4,5,6). State its shape, its entry in row two column three, and the rows of its transpose.\n\nIts shape is two by three, and the specified entry is 6. Its transpose is three by two with rows (1,4), (2,5), and (3,6).",
    "confusion": "The first entry index names a row. A superscript T denotes transpose, not a numerical power.",
    "lesson": "linear-algebra-matrices",
    "section": "Shape, entries, and transpose",
    "related": ["linear-algebra-matrices-2"]
  },
  {
    "id": "linear-algebra-matrices-2",
    "kind": "term",
    "name": "Matrix-vector product",
    "aliases": ["Matrix-vector product"],
    "linkAliases": [],
    "quick": "A vector of row dot products, equivalently a linear combination of columns.",
    "definition": "For the matrix A above and x=(2,1), $Ax=\\begin{pmatrix}4\\\\10\\end{pmatrix}$. The first output is the dot product of row (1,2) with x; the second is the dot product of row (3,4) with x. An m by n matrix therefore accepts an n-component column and returns an m-component column.\n\nThere is an equally useful column reading. Multiply the first column of A by the first component of x and the second column by the second component, then add. Here two copies of (1,3) plus one copy of (2,4) give (4,10). The row reading calculates each output; the column reading explains how the output is built.\n\nAn [identity matrix](ref:linear-algebra-matrices-2) leaves every compatible vector unchanged. In two dimensions it has rows (1,0) and (0,1). It is written $I_2$, with the subscript giving its size. More generally, an [identity matrix](ref:la-term-identity-matrix) has ones on its main diagonal, the entries whose row and column numbers agree, and zeros elsewhere.\n\n### One calculation, two explanations\n\nApply the two-by-three matrix with rows (1,2,0),(-1,3,4) to (2,-1,3). Row dot products give zero and seven. As columns, the calculation is twice (1,-1), minus (2,3), plus three times (0,4), again (0,7). The three input entries are coefficients; the two output entries report the resulting measurements.\n\nFor a workshop, the columns might give each product's resource requirements and the input its production quantities. For a dataset, each row might instead describe one observation and the input a list of weights used to predict an output. The same multiplication has different interpretations; always say what rows, columns, and entries mean in the application.",
    "example": "Let A have rows (1,2),(3,-1). Compute Ax for x=(2,3) using both the row-dot-product and column-combination readings.\n\nRow dot products give 2+6=8 and 6-3=3. The column reading gives two times (1,3) plus three times (2,-1), again (8,3).",
    "confusion": "The input needs one component per matrix column, and the output has one component per row.",
    "lesson": "linear-algebra-matrices",
    "section": "A matrix acting on a vector",
    "related": ["linear-algebra-matrices-1"]
  },
  {
    "id": "linear-algebra-matrices-3",
    "kind": "term",
    "name": "Matrix product",
    "aliases": ["Matrix product"],
    "linkAliases": [],
    "quick": "The matrix representing successive compatible transformations, with the right factor acting first.",
    "definition": "Suppose B first transforms an input vector and A then transforms the result. The combined matrix is AB, with B acting first. The product exists when the number of columns of A equals the number of rows of B. If A is m by n and B is n by p, AB is m by p.\n\nEach column of AB is A applied to the corresponding column of B. Equivalently, each entry is the dot product of a row of A with a column of B. This rule is not an arbitrary complication: it makes $A(Bx)=(AB)x$ hold for every compatible input x. Parentheses can change the order in which we carry out the calculation without changing which transformation acts first.\n\nFor A with rows (1,2),(3,4) and B with rows (2,0),(0,1), AB has rows (2,2),(6,4). The first column of A is doubled because B doubles the first input coordinate. BA instead has rows (2,4),(3,4): applying B afterward doubles the first output coordinate. These are different operations, so [matrix multiplication](ref:linear-algebra-matrices-3) is generally not commutative.\n\n### Building a product one column at a time\n\nLet A have rows (1,2,0),(-1,3,4), and let B have rows (1,0),(0,1),(1,-1). The first column of B is (1,0,1), which A sends to (1,3). The second is (0,1,-1), which A sends to (2,-1). Therefore AB has rows (1,2),(3,-1). This is a two-by-two product even though neither original matrix is two by two.\n\n$\n\\begin{pmatrix}1&2&0\\\\-1&3&4\\end{pmatrix}\\begin{pmatrix}1&0\\\\0&1\\\\1&-1\\end{pmatrix}=\\begin{pmatrix}1&2\\\\3&-1\\end{pmatrix}\n$\n\nTransposition reverses a product's order: the transpose of AB is B transpose times A transpose. The shape check already suggests why: transposing changes the output-by-input shape into input-by-output. Entry by entry, both sides pair the same row-column numbers. Keeping the original order after transposition is a common error.",
    "example": "Let A have rows (1,1),(0,1), and let B have rows (2,0),(0,1). Compute AB and BA and state which transformation acts first in ABx.\n\nAB has rows (2,1),(0,1), while BA has rows (2,2),(0,1). In ABx, B acts first and A acts on B's output. The two products are unequal.",
    "confusion": "Reversing order can change the result or make the product undefined.",
    "lesson": "linear-algebra-matrices",
    "section": "Composition determines the product",
    "related": ["linear-algebra-matrices-2"]
  },
  {
    "id": "linear-algebra-matrices-4",
    "kind": "term",
    "name": "Matrix cancellation",
    "aliases": ["Matrix cancellation"],
    "linkAliases": [],
    "quick": "Cancellation requires appropriate invertibility; it is not a general rule for matrices.",
    "definition": "Always check dimensions first. A two by three matrix times a three by four matrix gives a two by four result. Reversing their order would require matching four with two, so that reversed product is not defined. “Not defined” is different from “defined but unequal.”\n\nThen check a strategically chosen input. The first standard coordinate vector extracts the first column of a matrix; the second extracts the second column. If you claim a product has a particular first column, you can verify it by applying the transformations successively to the first standard vector.\n\nMatrix multiplication distributes over addition when the shapes match, but familiar scalar shortcuts need care. A product of two nonzero matrices can be the zero matrix, and an equation involving a matrix factor does not automatically permit [cancellation](ref:linear-algebra-matrices-4). We will explain exactly when a matrix can be undone in the lesson on bases and inverses.\n\nWhen typing matrices, use `\\begin{pmatrix}` and `\\end{pmatrix}`, separate entries in a row with `&`, and separate rows with `\\\\`. Check the preview's shape as well as its entries. A missing row separator can turn a correct list of numbers into the wrong mathematical object.\n\n### A useful counterexample to cancellation\n\nLet A keep the first coordinate and erase the second, so its rows are (1,0),(0,0). Inputs (3,1) and (3,8) both give (3,0). Thus equal outputs under A do not imply equal inputs. The same lost direction makes it possible to have AB=AC with B different from C.\n\nTesting one input can disprove a claimed matrix equality, but usually cannot prove it. Testing every standard basis input does prove equality of two linear matrix rules: these inputs recover all columns, and every vector is a combination of the standard basis. Explain which kind of check you are performing. The zero input alone tells you nothing, since every matrix sends it to zero.",
    "example": "A has rows (1,0),(0,0), and B has rows (0,0),(0,1). Compute AB. What scalar-style inference does this example disprove?\n\nAB is the two by two zero matrix even though neither factor is zero. A zero product of matrices does not imply that at least one factor is the zero matrix.",
    "confusion": "Two nonzero matrices can have zero product. Do not import scalar cancellation without checking its hypothesis.",
    "lesson": "linear-algebra-matrices",
    "section": "Checking a matrix calculation",
    "related": ["linear-algebra-matrices-3"]
  },
  {
    "id": "linear-algebra-systems-1",
    "kind": "term",
    "name": "Linear system",
    "aliases": ["Linear system"],
    "linkAliases": [],
    "quick": "A collection of linear equations that one assignment of unknowns must satisfy simultaneously.",
    "definition": "Consider $x+y=3$ together with $2x-y=0$. Each equation describes a line in the plane, and a solution is a point on both lines. Adding the equations gives three x equals three, so x is one; substituting gives y equal to two. Checking both original equations confirms the answer.\n\nThe coefficient matrix contains the coefficients of the unknowns, while the right-hand-side vector contains the constants. Writing the system as $Ax=b$ treats x as a column of unknowns and b as the required output. In this notation x is a vector, unlike the scalar x used in the two displayed equations above.\n\nAn [augmented matrix](ref:linear-algebra-systems-1) places the right-hand-side column beside the coefficient columns, usually separated visually by a bar. It records the same system compactly. Keep the variable order fixed: if the columns mean x then y, they must keep that meaning through every row operation.\n\n### Reading a missing coefficient\n\nThe equations x+2z=4 and y-z=1 have augmented rows (1,0,2,4) and (0,1,-1,1) in variable order x,y,z. The missing y in the first equation requires a zero placeholder. Omitting that entry would shift the z coefficient into the wrong column and encode a different system.\n\nTwo equations do not automatically mean one solution or two unknowns. The coefficient relationships matter. In three dimensions, one nonzero linear equation describes a plane; several equations ask for their common points. We will use elimination to distinguish an empty intersection, one point, and a family.",
    "example": "Solve x+y=5 and x-y=1. Verify the same pair in both original equations.\n\nAdding gives 2x=6, so x=3 and y=2. The checks are 3+2=5 and 3-2=1. A solution must use the same values in both equations.",
    "confusion": "Use a fixed variable order throughout an augmented matrix. Solutions must satisfy every original equation.",
    "lesson": "linear-algebra-systems",
    "section": "Constraints and augmented matrices",
    "related": ["linear-algebra-systems-2"]
  },
  {
    "id": "linear-algebra-systems-2",
    "kind": "term",
    "name": "Pivot and elimination",
    "aliases": ["Pivot and elimination"],
    "linkAliases": [],
    "quick": "A pivot is a leading nonzero entry used to eliminate other entries in its column.",
    "definition": "Three elementary row operations preserve the solution set: swap two equations, multiply an equation by a nonzero number, or add a multiple of one equation to another. Each operation can be undone, which is why it cannot gain or lose solutions. Multiplying an entire equation by zero would erase information and is not allowed.\n\nGaussian elimination uses these operations to remove coefficients below successive leading entries. A [pivot](ref:linear-algebra-systems-2) is a leading nonzero entry chosen to eliminate other entries in its column. In [row echelon form](ref:la-term-echelon-form), all zero rows are at the bottom, each nonzero row's leading entry is farther right than the one above, and entries below each leading entry are zero.\n\nFor the system above, replace the second row by the second row minus twice the first. The second equation becomes negative three y equals negative six. Solve it first, then substitute upward; this is back substitution. [Reduced row echelon form](ref:la-term-reduced-echelon-form) goes further: each leading entry is one, and all other entries in its column are zero. Either form can be used to solve a system.\n\n### Carrying a three-variable calculation through\n\nConsider x+y+z=6, 2x+3y+z=11, and x+2y+3z=14. Subtract twice the first row from the second to obtain y-z=-1. Subtract the first from the third to obtain y+2z=8. Subtract the new second row from the new third to obtain 3z=9. Back substitution gives z=3, y=2, and x=1. Checking all three original equations gives 6,11,14 as required.\n\nRecord each row operation before replacing entries, and include the right-hand side in it. Row swaps are allowed whenever a convenient pivot is missing. Swapping variable columns is different: it changes the variable order and must be tracked explicitly. We do not silently swap columns during elimination.\n\n\n$\n\\begin{pmatrix}1&1&1&6\\\\0&1&-1&-1\\\\0&0&3&9\\end{pmatrix}\n$",
    "example": "Solve x+2y=4 and 3x+5y=11 by replacing the second equation with the second minus three times the first. Show the resulting equation.\n\nThe new second equation is -y=-1, so y=1. Substitution into the first gives x=2. The original equations check as 2+2=4 and 6+5=11.",
    "confusion": "Multiplying a row by zero is irreversible and may change the solution set. Row echelon form need not be reduced row echelon form.",
    "lesson": "linear-algebra-systems",
    "section": "Elimination preserves solutions",
    "related": ["linear-algebra-systems-1"]
  },
  {
    "id": "linear-algebra-systems-3",
    "kind": "term",
    "name": "Free variable and consistency",
    "aliases": ["Free variable and consistency"],
    "linkAliases": [],
    "quick": "A consistent system has a solution; a non-pivot variable can parameterize its remaining freedom.",
    "definition": "A system is consistent if it has at least one solution. An equation such as zero equals one is impossible, so an augmented row with all zero coefficient entries and a nonzero last entry proves inconsistency. A row representing zero equals zero adds no restriction and does not itself indicate failure.\n\nFor example, x+y=3 and twice x plus twice y equals six describe the same line. One variable may be chosen freely. Writing $y=t,\\quad x=3-t$ describes every solution, with t an arbitrary real [parameter](ref:la-term-parameter). A parameter is a symbol that ranges over allowed values to describe a family of objects.\n\nA variable whose column has a pivot is a pivot variable; a non-pivot variable is free in a [consistent system](ref:la-term-consistent-system). Express pivot variables in terms of the free ones. Over the real numbers, a consistent linear system has either one solution, when there are no free variables, or infinitely many, when at least one variable is free. A [free variable](ref:linear-algebra-systems-3) alone does not rescue an inconsistent system.\n\n### Parameters can change the kind of solution\n\nIn the system x+y=2 and 2x+ky=c, subtract twice the first equation from the second. The remaining equation is (k-2)y=c-4. If k differs from two, division gives one y and then one x. If k equals two, division is forbidden: the equation is either 0=0, when c=4, or a contradiction, when c differs from four. The first case leaves one free parameter; the second has no solution.\n\nFor more than one free variable, introduce a separate real parameter for each. The equations x+2z-w=3 and y-z=1 give z=s, w=t, x=3-2s+t, y=1+s. Both s and t range independently over all real numbers. Choosing a single parameter for both would discard valid solutions.",
    "example": "Classify the system x+y=2 and 2x+2y=5 as having no solution, one solution, or infinitely many. Justify using elimination.\n\nThere is no solution. Subtracting twice the first equation from the second yields 0=1, which is impossible.",
    "confusion": "Zero equals zero gives no restriction; zero equals a nonzero constant makes the whole system inconsistent.",
    "lesson": "linear-algebra-systems",
    "section": "No solution or a family of solutions",
    "related": ["linear-algebra-systems-2"]
  },
  {
    "id": "linear-algebra-systems-4",
    "kind": "term",
    "name": "Homogeneous system",
    "aliases": ["Homogeneous system"],
    "linkAliases": [],
    "quick": "A linear system with zero right-hand side, always solved by the zero vector.",
    "definition": "A [homogeneous system](ref:linear-algebra-systems-4) has a zero right-hand side: $Ax=0$. Here zero denotes the zero output vector of the appropriate size. Such a system is always consistent, because the zero input works. A nontrivial solution means a solution other than the zero vector; the adjective says nothing about how difficult it was to find.\n\nIf p is one solution of Ax=b and z solves Az=0, then p+z is another solution of Ax=b, because matrix multiplication distributes over addition. Conversely, subtracting any two solutions of Ax=b gives a solution of the homogeneous system. Thus the homogeneous solutions describe the freedom remaining after one particular solution has been found.\n\nFor the equation x+y=3, one particular solution is (3,0), and homogeneous solutions are all multiples of (-1,1). This gives exactly the family written with t above. The connection between elimination and vector combinations is beginning to emerge.\n\nCheck a claimed family by substitution, and explain why it includes every solution rather than merely some solutions. In small examples, exact fractions are preferable to rounded decimals: an apparent tiny nonzero quantity caused by rounding should not be mistaken for an exact contradiction.\n\n### Why a parameterization is complete\n\nThe previous family can be written as (3,1,0,0) plus s times (-2,1,1,0) plus t times (1,0,0,1). The first vector is a particular solution; both direction vectors solve the associated homogeneous equations. Substitution proves that every displayed vector is a solution. Conversely, every solution has some values in its third and fourth coordinates, and the equations force its first two coordinates to have exactly this form. That converse establishes completeness.\n\nAn inconsistent system can have fewer pivots than unknowns and still have no solutions. Check for contradictions before assigning free parameters. A homogeneous system with more unknowns than equations, however, must have a free variable and a nonzero solution: it cannot be inconsistent, and there are too few rows to pivot in every variable column.",
    "example": "Find all solutions of x+y=0. Give one nontrivial solution and explain why the system must also have the zero solution.\n\nEvery solution is (-t,t) for real t. For instance (-1,1) is nontrivial. All homogeneous systems admit zero because multiplying zero inputs gives a zero output.",
    "confusion": "Nontrivial means nonzero, not difficult. A homogeneous system can have only its zero solution.",
    "lesson": "linear-algebra-systems",
    "section": "Homogeneous systems and verification",
    "related": ["linear-algebra-systems-3"]
  },
  {
    "id": "linear-algebra-span-1",
    "kind": "term",
    "name": "Span",
    "aliases": ["Span"],
    "linkAliases": [],
    "quick": "All real linear combinations of a supplied collection of vectors.",
    "definition": "The span of a collection of vectors is the set of all their linear combinations, using arbitrary real coefficients. We write $\\operatorname{span}\\{u,v\\}$ for the span of u and v, read “the span of u and v.” Braces specify the supplied collection, while span denotes all outputs obtainable from it. The vectors in this lesson belong to a fixed real coordinate space.\n\nThe span of a single nonzero vector is a line through the origin. The span of (1,0) and (0,1) is the whole coordinate plane. The span of (1,1) and (2,2) is only the diagonal line through (1,1): the second vector gives no direction that the first could not already produce. The zero vector belongs to every span, because all coefficients can be zero.\n\nTo test whether b lies in a given span, place the supplied vectors in the columns of a matrix A and solve Ax=b. A solution provides coefficients that build b. An inconsistent system proves that no such combination exists. This turns a geometric reachability question into a calculation you already know.\n\n### Describing an entire set\n\nThe span of (1,0,2) and (0,1,-1) consists of vectors (a,b,2a-b) for arbitrary real a,b. This description gives both a way to build members and a membership test: the third coordinate must equal twice the first minus the second. The vector (2,3,1) belongs, while (2,3,0) does not. A few plotted arrows cannot replace this all-coefficients argument.\n\nThe span of an empty collection is defined to be the zero-only subspace: the empty sum is zero. Adding the zero vector to a spanning collection changes nothing, and adding any vector already in its span changes nothing. To show two collections have equal spans, show each supplied vector in either collection can be built from the other collection.",
    "example": "Does (3,4) lie in the span of u=(1,0) and v=(1,2)? Find coefficients or prove none exist.\n\nYes. The second coordinate forces the coefficient of v to be 2. The first then forces the coefficient of u to be 1, so u+2v=(3,4).",
    "confusion": "The span is a set of outputs, not the original short list of building blocks. Always specify the ambient space when claiming full coverage.",
    "lesson": "linear-algebra-span",
    "section": "Span is everything you can build",
    "related": ["linear-algebra-span-2"]
  },
  {
    "id": "linear-algebra-span-2",
    "kind": "term",
    "name": "Linear independence",
    "aliases": ["Linear independence"],
    "linkAliases": [],
    "quick": "Only all-zero coefficients give a zero linear combination.",
    "definition": "A collection is [linearly independent](ref:linear-algebra-span-2) if the only linear combination producing the zero vector has every coefficient zero. It is linearly dependent if a zero combination exists with at least one nonzero coefficient. “Nonzero combination” here refers to the coefficients; the resulting vector is zero.\n\nWhy test combinations that give zero? If two different coefficient lists produce the same output, subtracting the two equations gives a zero combination with some nonzero coefficient. Conversely, a nonzero coefficient in a zero combination allows one vector to be expressed using the others. Dependence is therefore exactly a form of redundancy.\n\nFor u=(1,1) and v=(2,2), $2u-v=0$ exhibits dependence. For the standard vectors (1,0) and (0,1), a zero combination forces each coordinate coefficient to zero, so they are independent. Any collection containing the zero vector is dependent: put coefficient one on that vector and zero on all the others.\n\n### Pairwise tests do not settle a whole collection\n\nThe vectors (1,0), (0,1), and (1,1) are dependent although no pair is a scalar multiple. The third equals the sum of the first two. Dependence concerns the whole list, not just pairs. More generally, more than n vectors in real n-dimensional coordinate space must be dependent: the homogeneous coefficient system has more unknowns than rows.\n\n\n\nConversely, deleting vectors from an independent collection preserves independence. Any nontrivial zero combination among the shorter list would extend to one among the original list by placing zeros on the omitted vectors. Adding a vector outside the old span preserves independence: a nonzero coefficient on the new vector in a zero combination would express it using the old ones, a contradiction.",
    "example": "Determine whether u=(1,2), v=(2,4) are linearly independent. Give a zero combination that supports your conclusion.\n\nThey are dependent because 2u-v=0 has coefficients 2 and -1, not both zero. The vector v supplies no direction beyond u.",
    "confusion": "The output in the defining test is zero. Nontrivial refers to a coefficient list that is not all zero.",
    "lesson": "linear-algebra-span",
    "section": "Independence means no redundant direction",
    "related": ["linear-algebra-span-1"]
  },
  {
    "id": "linear-algebra-span-3",
    "kind": "term",
    "name": "Subspace, null space, and column space",
    "aliases": ["Subspace, null space, and column space"],
    "linkAliases": [],
    "quick": "A subspace contains zero and is closed under addition and real scaling. Null and column spaces describe zero-producing inputs and reachable outputs.",
    "definition": "A [subspace](ref:linear-algebra-span-3) of a real coordinate space is a subset that contains zero and is closed under vector addition and real scalar multiplication. Closed means that performing the operation on members stays inside the subset. These conditions let the subset function as a vector space with the inherited operations.\n\nEvery span is a subspace. Adding two combinations adds their coefficients, and scaling a combination scales its coefficients, so neither operation escapes the span. A line through the origin is an example; the line of points satisfying x+y=1 is not, because it does not contain zero.\n\nThe [null space](ref:la-term-null-space) of A is the set of solutions of Ax=0. It is a subspace of the input space: zero solves the equation, sums of solutions solve it, and scalar multiples solve it. The [column space](ref:la-term-column-space) of A is the span of its columns, hence the set of possible outputs. It is a subspace of the output space. These spaces need not have the same ambient dimension when A is rectangular.\n\n### Closing under both operations\n\nContaining zero is necessary but not sufficient. The union of the two coordinate axes contains zero and is closed under scaling, but adding (1,0) and (0,1) leaves the union. The intersection of two subspaces is a subspace: zero lies in both, and addition and scaling stay in both at once. An intersection means common members; a union means members of either set.\n\nThe [row space](ref:la-term-row-space) of a matrix is the span of its rows, viewed as vectors with one component per column. It belongs to the input coordinate space, unlike the column space. Row operations preserve the row space because each new row is a combination of old rows and the operations are reversible. They need not preserve the actual column space.",
    "example": "Is the set of plane vectors (x,y) satisfying x+y=0 a subspace? Check zero, addition, and scalar multiplication.\n\nYes. Zero satisfies the equation. Adding two pairs whose coordinate sums are zero gives another with sum zero. Scaling such a pair multiplies its zero coordinate sum by the scalar, leaving zero.",
    "confusion": "A line not through the origin is not a subspace. Null space belongs to the input space; column space belongs to the output space.",
    "lesson": "linear-algebra-span",
    "section": "Subspaces and the origin",
    "related": ["linear-algebra-span-2"]
  },
  {
    "id": "linear-algebra-span-4",
    "kind": "term",
    "name": "Independence versus spanning",
    "aliases": ["Independence versus spanning"],
    "linkAliases": [],
    "quick": "Independence ensures unique representations where they exist; spanning a specified space ensures existence there.",
    "definition": "Put the proposed vectors in the columns of A. They are [independent](ref:linear-algebra-span-4) exactly when Ax=0 has no free variables, since that is exactly when its only solution is zero. For example, columns (1,0,1) and (0,1,1) are independent: a combination producing zero forces its coefficients to zero by looking at the first two coordinates.\n\nThose two independent vectors do not span all three-dimensional space. Their combinations have third coordinate equal to the sum of the first two. They span a plane through the origin. Independence describes the absence of redundancy; spanning a specified space describes coverage. Always name the space you claim to span.\n\nIf you seek a target in this plane, its coefficients are unique. A target outside it has no representation. If you add a redundant third vector to the collection, some targets can have many representations without expanding the plane at all. The next lesson uses a basis to retain both coverage and uniqueness.\n\nWhen presenting a dependence argument, give actual coefficients or an explicit expression of one vector through the others. When presenting independence, explain why every zero combination forces all coefficients to vanish. A drawing can suggest the answer, but the component argument establishes it.\n\n### Turning a dependence into a useful explanation\n\nFor columns u=(1,0,1), v=(0,1,1), and w=(2,3,5), the relation w=2u+3v displays the redundant direction. Equivalently, coefficient vector (-2,-3,1) solves the homogeneous system. Both statements describe the same dependence. If a target has one representation, adding any multiple of this coefficient vector gives another representation of that same target.\n\n$\n\\begin{pmatrix}1&0&2\\\\0&1&3\\\\1&1&5\\end{pmatrix}\\begin{pmatrix}-2\\\\-3\\\\1\\end{pmatrix}=\\begin{pmatrix}0\\\\0\\\\0\\end{pmatrix}\n$\n\nThe null space consists of coefficient lists, not combinations in the output space. With three supplied vectors in the plane, a dependence witness has three components even though each supplied vector has only two. Keeping these spaces separate prevents a common dimensional error when interpreting elimination.",
    "example": "A matrix has three columns and its homogeneous system has one free variable. Are the columns independent? Explain using a nontrivial homogeneous solution.\n\nThey are dependent. Choosing a nonzero value for the free variable and solving for the pivot variables gives a nonzero coefficient vector sent to zero, a nontrivial zero combination of the columns.",
    "confusion": "An independent pair in three dimensions can span a plane without spanning the whole ambient space.",
    "lesson": "linear-algebra-span",
    "section": "Reading independence from elimination",
    "related": ["linear-algebra-span-3"]
  },
  {
    "id": "linear-algebra-bases-1",
    "kind": "term",
    "name": "Basis and dimension",
    "aliases": ["Basis and dimension"],
    "linkAliases": [],
    "quick": "A basis is an independent spanning list; dimension is its number of vectors.",
    "definition": "A basis of a subspace is an ordered list of vectors that is linearly independent and spans that subspace. Independence ensures uniqueness of the coefficient list; spanning ensures existence. Although the basis vectors themselves form a collection, we choose an order when using their coefficients as coordinates.\n\nFor the plane, p=(1,1) and q=(1,-1) form a basis. The vector (4,2) equals three p plus one q, so its coordinates in this ordered basis are (3,1). These are not its usual horizontal and vertical coordinates. Both lists describe the same vector relative to different building blocks.\n\nThe [dimension](ref:la-term-dimension) of a subspace is the number of vectors in a basis. A fundamental finite-dimensional result says that every basis of the same space has the same size. One way to understand why is an exchange argument: independent vectors can replace members of a spanning list one at a time, and there cannot be more independent replacements than available members. Applying this in both directions to two bases forces equal sizes.\n\nHere is the replacement argument more explicitly. Start with a spanning list of n vectors and an independent list u1 through um. Express u1 using the spanning list. Some coefficient is nonzero, so solve for that spanning vector and replace it by u1 without losing the span. At step j, express uj using the previous replacements and the remaining old vectors. Some coefficient on an old vector must be nonzero: otherwise uj would be a combination of u1 through u(j-1), contradicting independence. Replace that old vector. If m exceeded n, after n replacements the next independent vector would already lie in their span, again a contradiction. Thus an independent list has no more vectors than any spanning list. Two bases give the two opposite inequalities.\n\nThus the coordinate plane has dimension two, a line through the origin has dimension one, and the subspace containing only zero has dimension zero, with an empty basis. Two vectors in three-dimensional space can be a basis of a plane without being a basis of the entire space.\n\n### Extracting a basis rather than guessing one\n\nIf a spanning list contains a redundant vector, remove that vector without changing the span. Repeat until no redundancy remains; the resulting list is a basis. In the opposite direction, an independent list can be extended toward a desired finite-dimensional space by adding a vector outside its current span. Each addition expands the span while preserving independence.\n\nFor vectors (1,0,1), (0,1,1), and (1,1,2), the first two already span the third. They form a basis of the plane whose third coordinate is the sum of the first two, with dimension two. They do not form a basis of the full three-dimensional ambient space. A basis statement always names the space.",
    "example": "For the ordered basis p=(1,1), q=(1,-1), find the coordinates of (5,1). Distinguish them from standard coordinates.\n\nSolving a+b=5 and a-b=1 gives a=3, b=2. The coordinates in the ordered basis (p,q) are (3,2); standard coordinates remain (5,1).",
    "confusion": "Coordinates depend on the ordered basis. A plane in three-dimensional space still has dimension two.",
    "lesson": "linear-algebra-bases",
    "section": "A basis gives unique coordinates",
    "related": ["linear-algebra-bases-2"]
  },
  {
    "id": "linear-algebra-bases-2",
    "kind": "term",
    "name": "Rank and nullity",
    "aliases": ["Rank and nullity"],
    "linkAliases": [],
    "quick": "Rank counts independent output directions; nullity counts independent zero-producing input directions.",
    "definition": "The [rank](ref:la-term-rank) of a matrix is the dimension of its column space, the number of independent output directions its columns supply. Elimination finds it by counting pivot columns. To obtain a basis of the column space, take the pivot-indexed columns of the original matrix, not the changed columns of its row-reduced form. Row operations preserve dependencies among columns but can change the column space itself.\n\nThe [nullity](ref:la-term-nullity) is the dimension of the null space, or equivalently the number of free variables in the homogeneous system. For a matrix with n columns, $\\operatorname{rank}(A)+\\operatorname{nullity}(A)=n$. Read this as “rank of A plus nullity of A equals n.” Every input variable is either a pivot variable or a free variable, which explains the count.\n\nFor the matrix with columns (1,0), (0,1), and (1,1), rank is two and nullity is one. The third column adds no output direction, and coefficients (-1,-1,1) give a null-space direction. The matrix reaches every two-component output but loses some information about the three-component input.\n\n### Finding both spaces in one reduction\n\nTake A with rows (1,2,0),(0,0,1). The pivot columns are first and third, so a column-space basis is (1,0),(0,1). For the null space, the equations are x+2y=0 and z=0. Letting y=t gives all multiples of (-2,1,0). Rank is two, nullity one, and their sum is the three input coordinates.\n\n$\n\\begin{pmatrix}1&2&0\\\\0&0&1\\end{pmatrix}\\begin{pmatrix}-2\\\\1\\\\0\\end{pmatrix}=\\begin{pmatrix}0\\\\0\\end{pmatrix}\n$\n\nThe nonzero rows of a row-echelon form form a basis of the row space. Their distinct leading positions make them independent, and reversible row operations preserve their span. Consequently row-space dimension equals column-space dimension: both count pivots. For an m-by-n matrix, rank is at most the smaller of m and n. A full-column-rank matrix has rank n; a full-row-rank matrix has rank m.",
    "example": "A matrix has five columns and three pivots. Find its rank and nullity. What do the free variables describe?\n\nRank is three and nullity is two. The two free variables parameterize the homogeneous solutions, describing input freedom that produces no output change.",
    "confusion": "Their sum is the number of columns. Select a column-space basis from original columns at pivot indices.",
    "lesson": "linear-algebra-bases",
    "section": "Rank and nullity",
    "related": ["linear-algebra-bases-1"]
  },
  {
    "id": "linear-algebra-bases-3",
    "kind": "term",
    "name": "Matrix inverse",
    "aliases": ["Matrix inverse"],
    "linkAliases": [],
    "quick": "A matrix that undoes a square matrix, giving identity when multiplied in either order.",
    "definition": "A square matrix A is invertible if there is a matrix written $A^{-1}$ such that multiplying in either order gives the identity. The superscript negative one means matrix inverse here. It does not mean taking the reciprocal of each entry.\n\nThe inverse undoes A. If Ax=b, multiply on the left by the inverse to obtain x equal to A inverse times b. An n by n matrix is invertible exactly when its rank is n: elimination then leaves no free variables and permits every right-hand side. Its columns consequently form a basis of the full output space.\n\nThe diagonal matrix with diagonal entries 2 and 3 is inverted by the diagonal matrix with entries one half and one third. More generally, for a two by two matrix with rows (a,b),(c,d), the determinant is the scalar $ad-bc$. If it is nonzero, the inverse has rows (d,-b),(-c,a), all divided by that determinant. Multiplying the proposed inverse by the original verifies the formula. If the determinant is zero, elimination gives deficient rank and there is no inverse.\n\n### What a determinant measures in the plane\n\nFor columns (a,c) and (b,d), the absolute value of ad-bc is the area of the parallelogram they form. Its sign records orientation relative to the standard axes. Zero means the parallelogram collapses into a line or point, precisely the loss of an independent direction that prevents a two-by-two inverse. Area scaling describes this [two-dimensional determinant](ref:la-term-determinant-plane); it is not the length scaling of every vector.\n\nFor rows (2,1),(1,1), the determinant is one and the inverse has rows (1,-1),(-1,2). The first product row is (2-1,-2+2)=(1,0), and the second is (1-1,-1+2)=(0,1). This verifies the proposed inverse directly. The inverse of a product AB of invertible square matrices is B inverse times A inverse: undo the last action first.",
    "example": "Find the inverse of the matrix with rows (1,2),(0,1), and verify it by multiplication.\n\nThe inverse has rows (1,-2),(0,1). Multiplying in either order yields diagonal entries one and off-diagonal entries zero, the identity.",
    "confusion": "The inverse is not formed by reciprocating entries. A zero two-by-two determinant prevents invertibility.",
    "lesson": "linear-algebra-bases",
    "section": "Invertible matrices",
    "related": ["linear-algebra-bases-2"]
  },
  {
    "id": "linear-algebra-bases-4",
    "kind": "term",
    "name": "Existence versus uniqueness",
    "aliases": ["Existence versus uniqueness"],
    "linkAliases": [],
    "quick": "Reaching every target and reaching targets in only one way are separate matrix properties.",
    "definition": "To compute an inverse by elimination, augment A with an identity matrix and row-reduce the left side to identity. The same operations transform the right side into the inverse. If the left side cannot become identity, the inverse does not exist.\n\nFor solving one system, however, applying elimination directly to its right-hand side is usually the more natural method. Knowing that an inverse exists is a structural statement; explicitly forming it is a separate computational choice. Numerical software often solves systems without constructing an inverse.\n\nRank also separates [uniqueness](ref:linear-algebra-bases-4) from existence. A rectangular matrix can have independent columns and therefore at most one solution for each output, while still missing some outputs. A matrix can reach every output and still admit many inputs for each. Only in the square full-rank case do these two requirements combine into invertibility.\n\nWhen naming a basis, specify its space. When naming coordinates, specify the ordered basis. When counting rank, distinguish the number of rows or columns from the number of pivots: the whole point is that a large array can contain less independent information than its size suggests.\n\n### Seeing inverse elimination as solving several systems\n\nFor A with rows (1,2),(0,1), start with augmented rows (1,2,1,0) and (0,1,0,1), where the last two columns are identity. Subtract twice row two from row one. The left side becomes identity and the right side has rows (1,-2),(0,1), the inverse. Each right-hand column has solved the same coefficient system with a different standard basis output.\n\nThe number of equations alone cannot decide existence or uniqueness. Full column rank means no two inputs share an output; full row rank means every declared output is reachable. In a square matrix these ranks coincide. For rectangular matrices they answer different questions, and a two-sided inverse of the usual kind is unavailable. A one-sided inverse can exist, but is a different claim that must specify the multiplication order.",
    "example": "A three by two matrix has two independent columns. Can every three-component target have a solution? Can a target have two different solutions? Explain.\n\nIts column space has dimension two, so it cannot reach every three-component target. Its null space is zero, so any reachable target has exactly one solution. Existence and uniqueness are different questions.",
    "confusion": "A full-column-rank rectangular matrix may miss outputs. Explicitly computing an inverse is not necessary to solve one system.",
    "lesson": "linear-algebra-bases",
    "section": "Solving without unnecessary inverses",
    "related": ["linear-algebra-bases-3"]
  },
  {
    "id": "linear-algebra-transformations-1",
    "kind": "term",
    "name": "Linear transformation",
    "aliases": ["Linear transformation"],
    "linkAliases": [],
    "quick": "A function preserving vector addition and real scalar multiplication.",
    "definition": "A transformation is a function from one vector space to another. It is linear if it preserves addition and scalar multiplication. For every input pair u,v and every real scalar c, it must satisfy $T(u+v)=T(u)+T(v)$ and $T(cu)=cT(u)$. T names the function. These equations must hold for every allowed input, not just the examples we happen to test.\n\nEvery matrix rule T(x)=Ax is linear because matrix multiplication has these properties. Conversely, every [linear transformation](ref:linear-algebra-transformations-1) between real coordinate spaces has a matrix representation in standard coordinates. Its action on the standard basis determines everything: write an input as a combination of basis vectors and use linearity to move the coefficients through the transformation.\n\nIn particular, a linear transformation must take zero to zero. This is a useful way to disprove linearity, but passing the zero test is not sufficient. The function that squares each coordinate takes zero to zero yet fails to preserve addition.\n\n### Proving the properties for a whole rule\n\nFor T(x,y)=(2x-y,3y), applying T to the sum of (x,y) and (a,b) gives (2x+2a-y-b,3y+3b). This equals the sum of their individual outputs. Applying T to (cx,cy) gives (2cx-cy,3cy), which is c times the original output. Both identities hold for arbitrary real coordinates and c, so the rule is linear.\n\nFor S(x,y)=(x squared,y), the zero test passes. But S(1,0)+S(1,0)=(2,0), while S(2,0)=(4,0). One counterexample disproves additivity. A rule being simple, smooth, or zero-preserving is not a substitute for the two defining properties.",
    "example": "Prove that T(x,y)=(2x-y,3y) is linear by checking addition and scalar multiplication for arbitrary inputs.\n\nFor inputs (x,y),(a,b), T(x+a,y+b)=(2x+2a-y-b,3y+3b), equal to the sum of their outputs. For any real c, T(cx,cy)=(c(2x-y),c(3y))=cT(x,y). Both required properties hold.",
    "confusion": "Preserving zero is necessary, not sufficient. Coordinate squaring is a counterexample to the converse.",
    "lesson": "linear-algebra-transformations",
    "section": "What linearity requires",
    "related": ["linear-algebra-transformations-2"]
  },
  {
    "id": "linear-algebra-transformations-2",
    "kind": "term",
    "name": "Columns as basis images",
    "aliases": ["Columns as basis images"],
    "linkAliases": [],
    "quick": "A linear rule's standard matrix has the images of standard basis vectors as columns.",
    "definition": "In two dimensions, the first column is where the first standard basis vector goes, and the second column is where the second goes. A transformation sending (1,0) to (2,0) and (0,1) to (1,1) therefore has columns (2,0) and (1,1). An arbitrary input (a,b) goes to (2a+b,b).\n\nA rotation through a right angle counterclockwise sends (1,0) to (0,1) and (0,1) to (-1,0). Its matrix has rows (0,-1),(1,0). Applying it to (2,1) gives (-1,2). This is a concrete way to check both the direction of rotation and the column convention without memorizing a sign pattern.\n\nA [shear](ref:linear-algebra-transformations-2) slides points horizontally by an amount proportional to their vertical coordinate. For example, the rule (a,b) goes to (a+b,b) fixes the horizontal axis and tilts the vertical basis direction. It is linear even though it changes angles. Linear does not mean “preserves all geometric features.”\n\n\n\n### The same method works between different dimensions\n\nA linear map from three input coordinates to two outputs might send the standard basis vectors to (1,0), (2,1), and (-1,3). Its matrix has these three columns, and its rule is (x,y,z) mapped to (x+2y-z,y+3z). The column method specifies the entire map, not just its behavior on three test cases, because every input is a combination of the standard basis.\n\nA reflection across the horizontal axis sends (x,y) to (x,-y); a projection onto that axis sends it to (x,0). The reflection preserves lengths and is reversible. The projection loses the vertical input and is not reversible. Both are linear, so linearity alone does not imply preservation of lengths or information.",
    "example": "A linear transformation sends (1,0) to (2,1) and (0,1) to (-1,3). State its matrix rows and its output on (3,2).\n\nThe matrix has rows (2,-1),(1,3), placing the given outputs in columns. The output is three times (2,1) plus two times (-1,3), or (4,9).",
    "confusion": "The images go in columns, not rows. A shear is linear even though it changes angles.",
    "lesson": "linear-algebra-transformations",
    "section": "Read the matrix from its columns",
    "related": ["linear-algebra-transformations-1"]
  },
  {
    "id": "linear-algebra-transformations-3",
    "kind": "term",
    "name": "Kernel and image",
    "aliases": ["Kernel and image"],
    "linkAliases": [],
    "quick": "The kernel consists of zero-producing inputs; the image consists of actual outputs.",
    "definition": "The [kernel](ref:la-term-kernel) of a linear transformation is the set of inputs sent to zero. The image is the set of outputs it actually reaches. For a matrix rule, these are respectively the null space and column space already studied. The domain specifies permitted inputs, while the codomain specifies the declared output space; the image may be smaller than the codomain.\n\nA linear transformation is injective, meaning distinct inputs have distinct outputs, exactly when its kernel contains only zero. If T(u)=T(v), linearity gives T(u-v)=0. A zero-only kernel forces u=v. Conversely, a nonzero vector in the kernel has the same output as zero and disproves injectivity.\n\nIt is surjective onto its stated codomain exactly when its image equals that codomain. The projection (a,b) goes to (a,0), considered as a function from the plane to the plane, is neither injective nor surjective. Its kernel is the vertical axis and its image the horizontal axis. Declaring the horizontal axis as the codomain changes the surjectivity answer but does not change the rule.\n\n### Recovering an input versus reaching an output\n\nConsider T(x,y,z)=(x+z,y). Its kernel consists of (-t,0,t), with t real; its image is the entire coordinate plane because (a,b,0) reaches any requested (a,b). It is surjective but not injective. In contrast, S(x,y)=(x,y,x+y) has zero-only kernel but misses (0,0,1), so it is injective but not surjective onto three-dimensional space.\n\nThese examples connect the function language to elimination. A nonzero kernel produces multiple inputs with the same output. A proper image produces outputs with no input. Changing the declared codomain may change surjectivity, but it does not remove information already lost in the kernel.",
    "example": "For T(x,y,z)=(x,y), find the kernel and image. State whether T is injective and surjective when its codomain is the coordinate plane.\n\nThe kernel is all (0,0,t), with real t; the image is the whole plane. It is not injective because the third coordinate is lost, but it is surjective onto the stated codomain.",
    "confusion": "Image need not equal codomain. Injectivity depends on the kernel; surjectivity depends on the declared codomain.",
    "lesson": "linear-algebra-transformations",
    "section": "Kernel, image, and information loss",
    "related": ["linear-algebra-transformations-2"]
  },
  {
    "id": "linear-algebra-transformations-4",
    "kind": "term",
    "name": "Change of basis and affine rules",
    "aliases": ["Change of basis and affine rules"],
    "linkAliases": [],
    "quick": "A basis change re-expresses the same vectors; an affine rule adds a fixed offset to a linear rule.",
    "definition": "For a linear transformation from a space to itself, changing a basis changes its coordinate matrix without changing the transformation itself. Suppose the columns of an invertible matrix P are a new input basis in standard coordinates. Then P converts new coordinates to standard coordinates, while its inverse converts back. If both input and output use that new basis, the transformed coordinate matrix is $P^{-1}AP$. The order follows the journey: convert in, apply A, convert back.\n\nComposition of linear transformations is linear, and its matrix product acts from right to left. This explains why rotating and then stretching need not agree with stretching and then rotating. Try tracking a single off-axis vector when the order is unclear.\n\nA rule Ax+b with a fixed offset vector b is called affine. It is linear precisely when b is zero. Translations are affine and preserve straight lines, but a nonzero translation does not preserve the origin. In everyday language “linear graph” can mean a straight line with an intercept; in linear algebra, the zero-preserving condition is essential.\n\nWhen you decide whether a rule is linear, either justify both defining properties for arbitrary inputs or give one counterexample to a required property. A handful of successful tests cannot prove a universal statement.\n\n### Tracking a change of coordinates step by step\n\nLet the ordered basis be p=(1,1), q=(1,-1), and let A double the first standard coordinate while leaving the second fixed. The vector p goes to (2,1), whose new-basis coefficients are (3/2,1/2). The vector q goes to (2,-1), whose coefficients are (1/2,3/2). Thus the matrix in the new basis has these columns. The same transformation has acquired off-diagonal entries solely because the measuring directions changed.\n\n$\n\\begin{pmatrix}3/2&1/2\\\\1/2&3/2\\end{pmatrix}\n$\n\nThe formula with the same P on both sides applies to an [endomorphism](ref:la-term-endomorphism), meaning a linear map from a space to itself, when the same new basis is used for input and output. With different spaces or separately chosen bases, use the input basis matrix on the right and the inverse output basis matrix on the left. Do not force a rectangular map into the square change-of-basis formula.",
    "example": "Is T(x,y)=(x+2,y-1) linear or affine but not linear? Explain using its value at zero.\n\nIt is affine but not linear. Its value at (0,0) is (2,-1), whereas a linear transformation must preserve zero. It is the identity linear rule plus a fixed nonzero offset.",
    "confusion": "A nonzero translation is affine, not linear. Read coordinate-conversion matrix products right to left.",
    "lesson": "linear-algebra-transformations",
    "section": "Coordinates, composition, and affine rules",
    "related": ["linear-algebra-transformations-3"]
  },
  {
    "id": "linear-algebra-projections-1",
    "kind": "term",
    "name": "Orthogonal projection",
    "aliases": ["Orthogonal projection"],
    "linkAliases": [],
    "quick": "The closest vector in a subspace; its residual is perpendicular to that subspace.",
    "definition": "Let u be a nonzero real vector and b a target in the same coordinate space. The [projection](ref:linear-algebra-projections-1) p of b onto the line spanned by u is $p=\\frac{b\\cdot u}{u\\cdot u}u$. Read this as “b dot u divided by u dot u, times u.” The denominator is positive because u is nonzero. The scalar coefficient tells us how far along the line to go.\n\nThe [residual](ref:la-term-residual) r is the difference b-p, pointing from the approximation to the target. Substitution into its dot product with u gives zero, so the residual is perpendicular to the permitted line. For b=(3,1) and u=(1,0), the projection is (3,0) and the residual (0,1).\n\nWhy does perpendicular mean closest? Any other point q on the line differs from p by a vector along the line, perpendicular to the residual. The Pythagorean theorem says the squared distance from b to q equals the squared residual length plus the squared distance from p to q. The extra term is nonnegative and vanishes only at p. This proves minimal distance rather than merely suggesting it from a picture.\n\n\n\n### A non-axis projection with exact arithmetic\n\nLet b=(3,0) and u=(1,2). Their dot product is three and u dot u is five, so the coefficient is 3/5. The projected vector is (3/5,6/5), and the residual b-p is (12/5,-6/5). Its dot product with u is 12/5-12/5=0. The residual's squared norm is 36/5. Reporting only the coefficient 3/5 would not report the point on the line.\n\nReplacing u by any nonzero scalar multiple gives the same line and the same projected vector. The coefficient changes to compensate for the changed building block. If b already lies on the line, its residual is zero. If b is perpendicular to the line, the projection is zero.",
    "example": "Project b=(4,2) onto the line spanned by u=(1,1). Find the residual and check its perpendicularity to u.\n\nThe coefficient is (4+2)/(1+1)=3. The projection is (3,3), and the residual is (1,-1). Its dot product with u is 1-1=0.",
    "confusion": "A nonzero line direction is required in the line formula. A nonzero residual is expected for a target outside the subspace.",
    "lesson": "linear-algebra-projections",
    "section": "Projecting onto a line",
    "related": ["linear-algebra-projections-2"]
  },
  {
    "id": "linear-algebra-projections-2",
    "kind": "term",
    "name": "Orthonormal basis",
    "aliases": ["Orthonormal basis"],
    "linkAliases": [],
    "quick": "A spanning independent list of unit vectors perpendicular to one another.",
    "definition": "An orthogonal collection has pairwise zero dot products between distinct vectors. It is orthonormal if each vector also has norm one. For an orthonormal basis q sub one through q sub k of a subspace, project b by adding, for each basis vector, b dot that vector times that vector. The unit lengths remove the denominators from the line formula.\n\nFor a matrix Q whose columns are these basis vectors, this becomes $p=QQ^Tb$. The transpose turns the columns into rows so that Q transpose times b computes the coefficients. The orthonormal condition is $Q^TQ=I$. The reverse product Q times Q transpose need not be the identity: it is the projection onto the column space, which may be smaller than the whole output space.\n\nGram–Schmidt is a procedure for turning an independent list into an orthonormal list spanning the same space. Keep the first vector's direction and normalize it. From the next vector, subtract its projections onto all the directions already retained, then normalize the remainder. Independence ensures the remainder is nonzero in exact arithmetic. Dependent input needs separate handling rather than division by zero.\n\n### Carrying out Gram–Schmidt\n\nStart with independent vectors a=(1,1,0) and b=(1,0,1). Normalize a to get q1=(1,1,0) divided by square root of two. The projection of b onto a's line is (1/2,1/2,0). Subtracting gives (1/2,-1/2,1), which is orthogonal to a. Normalize this remainder to obtain q2=(1,-1,2) divided by square root of six. Both vectors now have length one, their dot product is zero, and the original vectors can be rebuilt from them.\n\nThis construction also explains [QR factorization](ref:la-term-qr): for an independent-column matrix A, put the orthonormal directions in Q and their combination coefficients in an upper-triangular matrix R, so A=QR. Upper triangular means entries below the main diagonal are zero. We name QR here to explain the numerical-method reference later; the practice requires only the small Gram–Schmidt construction just shown.",
    "example": "Use Gram–Schmidt on u=(1,0) followed by v=(1,1). Give the resulting orthonormal vectors and show the subtraction step.\n\nThe first normalized direction is (1,0). The projection of v onto it is (1,0); subtracting leaves (0,1), already unit length. The resulting ordered list is (1,0),(0,1).",
    "confusion": "For rectangular Q with orthonormal columns, Q transpose Q is identity, but Q Q transpose may only be a projection.",
    "lesson": "linear-algebra-projections",
    "section": "Orthonormal directions",
    "related": ["linear-algebra-projections-1"]
  },
  {
    "id": "linear-algebra-projections-3",
    "kind": "term",
    "name": "Least squares",
    "aliases": ["Least squares"],
    "linkAliases": [],
    "quick": "Choose coefficients minimizing squared Euclidean distance between a model output and a target.",
    "definition": "For a matrix A and target b, a [least-squares](ref:linear-algebra-projections-3) solution chooses x to minimize the squared Euclidean norm of Ax-b. It does not usually satisfy every original equation. It selects the output in the column space nearest to b. The residual must be orthogonal to every column of A, giving $A^T(Ax-b)=0$. Rearranging produces the [normal equations](ref:la-term-normal-equations), $A^TAx=A^Tb$.\n\n“Normal” here means perpendicular; it does not refer to a probability distribution. These equations characterize least-squares solutions even when A has dependent columns. If the columns are independent, A transpose A is invertible and the coefficient solution is unique. If they are dependent, the closest output is still unique, but more than one coefficient vector can produce it.\n\nSuppose our model predicts the same number c for measurements 1, 2, and 6. A has one column of three ones. The normal equation is three c equals nine, giving c=3. The residual components sum to zero, so the residual is orthogonal to the constant-output direction. This derives the arithmetic mean as the best constant fit under squared error.\n\n### Fitting a line rather than a constant\n\nFit predicted values c+dt to observations (t,y)=(0,1),(1,2),(2,2). The [design matrix](ref:la-term-design-matrix), meaning the matrix of model coefficients, has rows (1,0),(1,1),(1,2). Its first column multiplies the intercept c; its second multiplies the slope d. The normal equations are 3c+3d=5 and 3c+5d=6. Subtraction gives 2d=1, so d=1/2 and c=7/6.\n\nThe fitted output is (7/6,5/3,13/6), and the residual, defined throughout as observations minus fitted output, is (-1/6,1/3,-1/6). Its dot product with (1,1,1) is zero; its dot product with (0,1,2) is also zero. These two checks establish the required orthogonality to every column. The squared error is 1/6, obtained by squaring and adding those three residuals.\n\n\n$\n\\begin{pmatrix}3&3\\\\3&5\\end{pmatrix}\\begin{pmatrix}c\\\\d\\end{pmatrix}=\\begin{pmatrix}5\\\\6\\end{pmatrix}\n$",
    "example": "Find the least-squares constant fit c to observations 2,4,9. Give the fitted output and the residual b minus the fitted output.\n\nThe normal equation is 3c=15, giving c=5. The fitted output is (5,5,5), and the residual is (-3,-1,4). Its components sum to zero, making it perpendicular to the column of ones.",
    "confusion": "The normal equations describe perpendicular residuals, not a normal probability distribution. Coefficients need not be unique when columns are dependent.",
    "lesson": "linear-algebra-projections",
    "section": "Least squares and the normal equations",
    "related": ["linear-algebra-projections-2"]
  },
  {
    "id": "linear-algebra-projections-4",
    "kind": "term",
    "name": "Residual and model interpretation",
    "aliases": ["Residual and model interpretation"],
    "linkAliases": [],
    "quick": "The target minus its fitted output measures what the model leaves unexplained.",
    "definition": "Least squares minimizes a specified measure of discrepancy. It does not prove a causal explanation or guarantee sensible predictions outside the observed range. Squaring residuals emphasizes large errors, and changing the relative scale of measured quantities can change the fitted result.\n\nFor another small example, approximate b=(2,0) on the line spanned by u=(1,1). Their dot product is 2 and u's squared length is 2, so the coefficient is one, the projection is (1,1), and the [residual](ref:linear-algebra-projections-4) is (1,-1). The original vector is not on the line; a nonzero residual is expected, not a failed calculation.\n\nThe normal equations are excellent for understanding the geometry and for hand calculations. Numerical software often uses QR factorization or SVD instead, because forming A transpose A can worsen sensitivity to rounding. We need not implement those algorithms here, but should distinguish a mathematical characterization from the best numerical recipe.\n\nIn a response, report the coefficient vector, the fitted output, and the residual when requested. They live in different spaces and answer different questions. Calling all three “the answer” can hide an otherwise correct calculation's meaning.\n\n### A fit can be unique even when its parameters are not\n\nIf A has two identical columns u, the fitted vector depends only on the sum of the two coefficients. Changing them by (t,-t) leaves the output unchanged. The least-squares fitted vector is still the unique closest point in the column space, but the coefficients are not unique. Inverting the normal-equation matrix in this situation is invalid; solve the consistent normal equations with free parameters instead.\n\nFor full independent columns, A transpose A is invertible because any vector sent to zero by it has zero squared output length under A and must itself be zero. This ties uniqueness to the rank criterion already established. It also distinguishes an exact algebraic theorem from a numerical warning: nearly dependent columns can make recovered coefficients very sensitive to small measurement changes even when exact uniqueness holds.",
    "example": "A least-squares calculation returns a nonzero residual. Does that alone mean the calculation is wrong? Explain.\n\nNo. If the target lies outside the column space, no exact fit exists and the closest permitted output has a nonzero residual. The residual should be orthogonal to the column space at a least-squares solution.",
    "confusion": "The best fit for an error measure does not establish causation or guarantee reliable extrapolation.",
    "lesson": "linear-algebra-projections",
    "section": "What a best fit does and does not say",
    "related": ["linear-algebra-projections-3"]
  },
  {
    "id": "linear-algebra-eigenvalues-1",
    "kind": "term",
    "name": "Eigenvalue and eigenvector",
    "aliases": ["Eigenvalue and eigenvector"],
    "linkAliases": [],
    "quick": "A nonzero vector sent to a scalar multiple of itself is an eigenvector; that scalar is its eigenvalue.",
    "definition": "For a square matrix A, a nonzero vector v is an [eigenvector](ref:linear-algebra-eigenvalues-1) if $Av=\\lambda v$ for some scalar lambda, its eigenvalue. Read the equation as “A times v equals lambda times v.” The Greek letter lambda names a number; v must be nonzero, since zero would satisfy the equation for every number and tell us nothing.\n\nFor the diagonal matrix with entries 2 and 3, the first standard vector is scaled by two and the second by three. Those are eigenvectors with different eigenvalues. The vector (1,1) is sent to (2,3), so it is not an eigenvector: no single scalar multiplies both original components to give the result.\n\nA negative eigenvalue reverses the vector as well as changing its length. A zero eigenvalue sends a nonzero vector to zero, so a matrix with zero as an eigenvalue is not invertible. Multiplying an eigenvector by any nonzero scalar produces another eigenvector for the same eigenvalue. An eigendirection therefore has many representative vectors.\n\n### Verifying a candidate before solving a polynomial\n\nLet A have rows (2,1),(1,2). It sends (1,1) to (3,3), so that direction has eigenvalue three. It sends (1,-1) to (1,-1), so that direction has eigenvalue one. The vectors are independent and perpendicular. For (2,0), the output (4,2) is not a scalar multiple, so this particular vector is not an eigenvector even though A has an eigenbasis.\n\nThe word eigen comes from German usage for something's own or characteristic quantity. The terminology points to directions adapted to this particular transformation. There is no preferred eigenvector length: after finding a direction you may normalize it, but normalization cannot turn a non-eigenvector into an eigenvector.",
    "example": "A has diagonal entries 2 and -1, with zeros off the diagonal. Which of (1,0), (0,1), and (1,1) are eigenvectors? Give each corresponding eigenvalue.\n\nThe first vector is an eigenvector with eigenvalue 2, and the second with eigenvalue -1. The third goes to (2,-1), which is not a single scalar multiple of (1,1), so it is not an eigenvector.",
    "confusion": "Zero may be an eigenvalue but not an eigenvector. Negative eigenvalues reverse direction without giving negative lengths.",
    "lesson": "linear-algebra-eigenvalues",
    "section": "Directions that are only scaled",
    "related": ["linear-algebra-eigenvalues-2"]
  },
  {
    "id": "linear-algebra-eigenvalues-2",
    "kind": "term",
    "name": "Eigenspace",
    "aliases": ["Eigenspace"],
    "linkAliases": [],
    "quick": "The null space of A minus lambda times identity for a specified eigenvalue lambda.",
    "definition": "Rearrange the defining equation to $(A-\\lambda I)v=0$. The identity has the same size as A. To have a nonzero solution v, the matrix A minus lambda times identity must be singular, meaning not invertible. For two by two matrices, its determinant must therefore be zero.\n\nTake A with rows (2,1),(0,3). Its determinant condition is $(2-\\lambda)(3-\\lambda)=0$, so the possible eigenvalues are 2 and 3. For lambda equal to 2, solving the homogeneous equations forces the second component of v to zero, giving direction (1,0). For lambda equal to 3, the components must agree, giving direction (1,1). Check each direction in the original matrix equation.\n\nFor a fixed eigenvalue, the [eigenspace](ref:linear-algebra-eigenvalues-2) is the null space of A minus lambda times identity. It includes zero so that it is a subspace, although zero itself is not an eigenvector. The determinant equation identifies candidate scalars; solving a homogeneous system identifies their vectors. Do not stop after finding the eigenvalues if the question also asks for directions.\n\n### From the polynomial back to directions\n\nFor the symmetric matrix with rows (2,1),(1,2), subtract lambda from both diagonal entries and compute the determinant. It is (2-lambda) squared minus one, which factors into (1-lambda)(3-lambda). These roots recover one and three. For lambda three, the homogeneous equations say y=x. For lambda one, they say y=-x. Thus each eigenspace is an entire line including zero.\n\n$\n\\det\\begin{pmatrix}2-\\lambda&1\\\\1&2-\\lambda\\end{pmatrix}=(1-\\lambda)(3-\\lambda)\n$\n\nThe determinant expression is called the [characteristic polynomial](ref:la-term-characteristic-polynomial). A root's [algebraic multiplicity](ref:la-term-algebraic-multiplicity) is the number of times its factor repeats in that polynomial. Its [geometric multiplicity](ref:la-term-geometric-multiplicity) is the dimension of its eigenspace. A repeated root does not tell you its eigenspace dimension; solving the homogeneous system is still necessary. Here both roots occur once.",
    "example": "Find the eigenvalues and a nonzero eigenvector for each for A with rows (4,1),(0,2). Show how the determinant condition and homogeneous equations are used.\n\nThe determinant of A-lambda I is (4-lambda)(2-lambda), giving eigenvalues 4 and 2. For 4, the second component is zero, so (1,0) works. For 2, 2x+y=0, so (1,-2) works. Direct multiplication confirms both.",
    "confusion": "An eigenspace contains zero, although eigenvectors exclude it. Finding eigenvalues alone does not identify the vector directions.",
    "lesson": "linear-algebra-eigenvalues",
    "section": "Finding eigenvalues in two dimensions",
    "related": ["linear-algebra-eigenvalues-1"]
  },
  {
    "id": "linear-algebra-eigenvalues-3",
    "kind": "term",
    "name": "Diagonalization",
    "aliases": ["Diagonalization"],
    "linkAliases": [],
    "quick": "Represent a square matrix using a basis of eigenvectors so that its coordinate matrix is diagonal.",
    "definition": "A matrix is [diagonalizable](ref:linear-algebra-eigenvalues-3) over the real numbers if it has a basis of real eigenvectors. Put those basis vectors in the columns of P and their matching eigenvalues on the diagonal of D. The equation $AP=PD$ records all the eigenvector equations at once. Since P is invertible, this gives $A=PDP^{-1}$.\n\nThis factorization has an operational reading: change from standard coordinates to eigenvector coordinates, scale each coordinate separately with D, then change back. Repeating A repeatedly cancels adjacent inverse changes of basis. For a positive integer k, $A^k=PD^kP^{-1}$. Powers of a diagonal matrix simply raise its diagonal entries to the corresponding powers.\n\nFor the triangular example above, (2,1) equals one copy of (1,0) plus one copy of (1,1). Applying A twice gives four copies of the first eigenvector plus nine copies of the second, or (13,9). This explains the repeated transformation without multiplying out A squared first.\n\n### Repeated action can include alternating signs\n\nIf an input is three u minus two v, with Au=2u and Av=-v, then after k applications its u coefficient is three times 2 to the k, and its v coefficient is negative two times (-1) to the k. The second component alternates sign instead of growing. For k=2 the output is twelve u minus two v. This follows by applying the same scaling at each step, not by assuming that ordinary component positions are eigenvector coordinates.\n\nEigenvectors with distinct eigenvalues are independent. For two vectors, if one were a nonzero multiple of the other, applying A would force their eigenvalues to agree. More generally the result extends to any finite list of distinct eigenvalues. Consequently a two-by-two real matrix with two distinct real eigenvalues has an eigenbasis. Repeated eigenvalues need an eigenspace calculation.",
    "example": "Independent vectors u and v form a basis, with Au=2u and Av=3v. Find A applied three times to the input u-2v, expressed in that basis.\n\nEach application scales the u coefficient by 2 and the v coefficient by 3. After three applications the result is 8u-54v. Linearity lets the two basis components be tracked separately.",
    "confusion": "Eigenvectors must be independent and form a full basis; a list with repeats cannot supply missing directions.",
    "lesson": "linear-algebra-eigenvalues",
    "section": "An eigenbasis simplifies repeated action",
    "related": ["linear-algebra-eigenvalues-2"]
  },
  {
    "id": "linear-algebra-eigenvalues-4",
    "kind": "term",
    "name": "Spectral theorem",
    "aliases": ["Spectral theorem"],
    "linkAliases": [],
    "quick": "Every real symmetric matrix has an orthonormal basis of real eigenvectors.",
    "definition": "Not every real square matrix has an eigenbasis. The matrix with rows (1,1),(0,1) has only eigenvalue one, but its eigenvectors lie on the horizontal axis. There are not enough independent eigenvectors to form a basis of the plane. Repeating an eigenvalue on a list does not supply a missing direction.\n\nA right-angle rotation has no real eigenvectors: every nonzero real direction turns to a perpendicular direction. Complex numbers extend the theory, but our calculations here stay over the real numbers. “No real eigenvector” is not the same claim as “no eigenvector over any number system.”\n\nA real symmetric matrix, meaning A transpose equals A, has an orthonormal eigenbasis. This [spectral theorem](ref:linear-algebra-eigenvalues-4) is a structural result we use without proving it here. It will underpin the next lesson's SVD. General matrices need not have perpendicular eigenvectors; the two directions in our triangular example are not perpendicular.\n\nEigenvalues can reveal growth or decay under repeated transformations when an eigenbasis is available. A component in a direction with eigenvalue two doubles each time; one with eigenvalue one half shrinks. Whether a particular input exhibits a direction's behavior depends on whether it has a nonzero component in that direction. Avoid inferring every trajectory from the largest eigenvalue alone without checking the assumptions.\n\n### Comparing two repeated-eigenvalue matrices\n\nThe identity has eigenvalue one twice and its eigenspace is the whole plane, so it is diagonalizable. The shear with rows (1,1),(0,1) also has eigenvalue one twice, but its eigenspace is just the horizontal axis, so it is not. Algebraic multiplicity is two in both examples; geometric multiplicity distinguishes them. In general, diagonalization requires enough independent eigenvectors to span the whole space.\n\nFor a symmetric matrix, eigenvectors belonging to different eigenvalues are perpendicular. To see the mechanism, compare u dot Av with Au dot v. Symmetry makes these equal; the eigenvector equations then give the difference of the eigenvalues times u dot v equal to zero. Distinct eigenvalues force a zero dot product. The stronger spectral theorem also handles repeated eigenspaces and guarantees a complete orthonormal basis; its full proof is beyond this lesson.",
    "example": "A has rows (1,1),(0,1). Determine its eigenvectors' directions and explain why it is not diagonalizable over the real numbers.\n\nIts only eigenvalue is one. Solving (A-I)v=0 forces the second component to zero, so all eigenvectors lie on the horizontal axis. There cannot be two independent eigenvectors to form a plane basis.",
    "confusion": "General real square matrices need not have an eigenbasis or even real eigenvectors. The symmetric hypothesis is essential.",
    "lesson": "linear-algebra-eigenvalues",
    "section": "Limits and interpretation",
    "related": ["linear-algebra-eigenvalues-3"]
  },
  {
    "id": "linear-algebra-svd-1",
    "kind": "term",
    "name": "Singular value decomposition",
    "aliases": ["Singular value decomposition"],
    "linkAliases": [],
    "quick": "Factor a real matrix into orthogonal input/output coordinate changes and nonnegative diagonal scale factors.",
    "definition": "For an m by n real matrix A, a [singular value decomposition](ref:linear-algebra-svd-1) is $A=U\\Sigma V^T$. Here U is an m by m [orthogonal matrix](ref:la-term-orthogonal-matrix), V is an n by n orthogonal matrix, and Sigma is an m by n rectangular diagonal matrix. Orthogonal matrix means a square matrix whose columns form an orthonormal basis; its transpose is its inverse. Sigma has nonnegative numbers on its diagonal and zero elsewhere.\n\nThe diagonal numbers are singular values, usually listed from largest to smallest. The columns of V are right singular vectors in the input space; columns of U are left singular vectors in the output space. The Greek capital Sigma names this matrix here, not a summation operation. We use the full SVD convention in this lesson; software may return smaller versions that omit unused directions.\n\nRead the product from right to left. V transpose expresses the input in perpendicular coordinates, Sigma scales those coordinates and may discard some, and U expresses the result in output coordinates. The orthogonal factors preserve Euclidean length; stretching and information loss occur in Sigma. Orthogonal factors can include reflections, so calling them only “rotations” would be too restrictive.\n\n### Rectangular factors need a dimension check\n\nAn m-by-n matrix has only min(m,n) diagonal positions in its rectangular Sigma. Here min means the smaller of the two numbers. If there are more inputs than outputs, some input basis directions have no diagonal position at all and are necessarily erased. For example, the two-by-three matrix with rows (3,0,0),(0,2,0) has listed singular values three and two but still has the third standard vector in its null space. Always compute nullity as number of columns minus rank, not just by counting zeros in a shortened singular-value list.\n\nFor a tall matrix, the extra rows of Sigma are zero and describe unused output directions. Orthonormal changes of coordinates do not alter vector length within their own spaces; the rectangular diagonal factor is the step that changes dimension. This is why the shapes of all three factors matter as much as their entries.",
    "example": "For a three by two matrix, state the shapes of U, Sigma, and V in the full SVD. Explain which factor contains the length scale factors.\n\nU is three by three, Sigma is three by two, and V is two by two. Sigma contains nonnegative singular values on its diagonal; the orthogonal U and V factors preserve length.",
    "confusion": "The full SVD uses square U and V and rectangular Sigma. Orthogonal factors can be reflections as well as rotations.",
    "lesson": "linear-algebra-svd",
    "section": "Perpendicular directions and scale factors",
    "related": ["linear-algebra-svd-2"]
  },
  {
    "id": "linear-algebra-svd-2",
    "kind": "term",
    "name": "Singular directions",
    "aliases": ["Singular directions"],
    "linkAliases": [],
    "quick": "Right singular vectors are input directions; matching left vectors give their output directions.",
    "definition": "For the matrix with rows (0,2),(1,0), choose V to swap the two standard basis directions, Sigma with diagonal entries 2 and 1, and U equal to the identity. Then V transpose swaps the input coordinates and Sigma scales the first resulting coordinate by two, giving (2y,x) from input (x,y), exactly as the original matrix does.\n\nThe right singular direction (0,1) is stretched by two into output direction (1,0). The right singular direction (1,0) is stretched by one into output direction (0,1). The [singular values](ref:linear-algebra-svd-2) describe nonnegative length factors even though the input and output directions differ. In particular, singular values are not generally eigenvalues.\n\nA semiaxis is the distance from the center of an ellipse to its boundary along one of its perpendicular principal directions.\n\n\n\nThe input unit circle is sent to an ellipse with horizontal semiaxis two and vertical semiaxis one. If a singular value were zero, the corresponding direction would collapse and the ellipse could flatten into a line segment. An ellipsoid is the higher-dimensional analogue of an ellipse, obtained by scaling a sphere along perpendicular directions. In more dimensions the same stretching idea describes an ellipsoid, although a plane drawing cannot show every direction.\n\n### Signs belong in the directions\n\nFor a diagonal matrix with entries -4 and 2, the singular values are four and two. Choose V as identity and U as the reflection with diagonal entries -1 and 1; Sigma then has entries four and two. Multiplying reconstructs the original signs. A negative matrix entry does not require a negative singular value.\n\nFor a rectangular matrix with rows (3,0),(0,2),(0,0), full U is three by three, full V two by two, and Sigma three by two. A unit circle in the input plane becomes an ellipse in the first two coordinates of the three-dimensional output. The third left basis vector completes the output basis but does not correspond to an additional positive singular value.",
    "example": "Let U be identity, Sigma have diagonal entries 3 and 1, and V swap the two standard basis vectors. Compute the rows of U Sigma V transpose and its output on (2,4).\n\nV transpose also swaps coordinates. The product has rows (0,3),(1,0), sending (2,4) to (12,2). The scale factors are 3 and 1 along the chosen right singular directions.",
    "confusion": "Input and output directions may differ. Singular values are nonnegative and generally differ from eigenvalues.",
    "lesson": "linear-algebra-svd",
    "section": "A small example you can verify",
    "related": ["linear-algebra-svd-1"]
  },
  {
    "id": "linear-algebra-svd-3",
    "kind": "term",
    "name": "Positive semidefinite matrix",
    "aliases": ["Positive semidefinite matrix"],
    "linkAliases": [],
    "quick": "A real symmetric matrix whose quadratic expression is nonnegative on every real vector.",
    "definition": "For every input vector x, $x^TA^TAx=\\|Ax\\|^2\\geq0$. The left side is a scalar quadratic expression: each term multiplies two input components, with a fixed matrix coefficient. The right side is squared length. Thus A transpose A is symmetric and [positive semidefinite](ref:linear-algebra-svd-3), meaning this quadratic expression is nonnegative for every real x. The spectral theorem supplies an orthonormal basis of eigenvectors for it, with nonnegative eigenvalues.\n\nThe right singular vectors can be chosen as those eigenvectors, and the singular values are the square roots of their eigenvalues. For a positive singular value sigma and corresponding unit right vector v, define the left vector by dividing Av by sigma. This produces the matching unit output direction. Zero singular values require completing orthonormal bases rather than dividing by zero. These facts explain why an SVD exists; they are not a prescription to implement a numerically stable algorithm by explicitly forming A transpose A.\n\nThe rank is the number of positive singular values. Each positive value supplies an output direction; each zero input direction lies in the null space. For exact mathematical data this count is exact. Floating-point software must decide which tiny values are effectively zero, so numerical rank depends on a tolerance, meaning a chosen threshold for treating a tiny computed value as zero, and on the scale of the problem.\n\n### The two products live in different spaces\n\nFor the two-by-three matrix with rows (3,0,0),(0,2,0), A transpose A is three by three with diagonal (9,4,0). A A transpose is two by two with diagonal (9,4). Both have the same positive eigenvalues, but they have different counts of zero eigenvalues. Their eigenvectors belong to input and output spaces respectively. This resolves the apparent mismatch between two listed positive singular values and a nontrivial null space.\n\n$\nA^TA=\\begin{pmatrix}9&0&0\\\\0&4&0\\\\0&0&0\\end{pmatrix},\\quad AA^T=\\begin{pmatrix}9&0\\\\0&4\\end{pmatrix}\n$\n\nFor positive singular values, the constructed left vectors are orthonormal: the dot product of Av and Aw equals v transpose times A transpose A times w, which vanishes for distinct chosen orthonormal right directions and becomes the squared singular value for matching directions. Dividing by the positive scale factors gives unit lengths. A basis for any remaining output directions is chosen perpendicular to these. Repeated singular values permit several valid orthonormal choices; an SVD is not generally unique.",
    "example": "A real matrix has singular values 4,2,0 and has three columns. Find its rank and nullity, and give the eigenvalues of A transpose A.\n\nRank is two because two singular values are positive. Nullity is one by rank plus nullity equals three. The eigenvalues of A transpose A are 16,4,0, the squares of the singular values.",
    "confusion": "For A transpose A the quadratic expression is squared output length. Zero singular values cannot be used as divisors.",
    "lesson": "linear-algebra-svd",
    "section": "Connection to rank and eigenvalues",
    "related": ["linear-algebra-svd-2"]
  },
  {
    "id": "linear-algebra-svd-4",
    "kind": "term",
    "name": "Truncated SVD and Frobenius norm",
    "aliases": ["Truncated SVD and Frobenius norm"],
    "linkAliases": [],
    "quick": "Retain the largest singular values for a low-rank approximation; Frobenius norm measures the square root of summed squared entries.",
    "definition": "A rank-k [truncated SVD](ref:la-term-truncated-svd) keeps the k largest singular values and replaces the others by zero. Here k is a nonnegative integer no larger than the rank. Rank-k means at most k independent output directions. This simplification can store or process an approximation using fewer directions.\n\nThe [Frobenius norm](ref:linear-algebra-svd-4) of a matrix is the square root of the sum of the squares of all its entries. A fundamental approximation theorem says that truncated SVD minimizes the [Frobenius norm](ref:la-term-frobenius) of the difference among matrices of rank at most k. We state this theorem without its proof. Its squared error equals the sum of the squares of the discarded singular values. Tied singular values can make the best approximation nonunique.\n\nFor a diagonal matrix with entries 4 and 1, keeping only the first direction gives diagonal entries 4 and 0. The Frobenius error is one. A nonzero component of the original transformation was discarded; this is a controlled approximation, not exact recovery. In image compression, the entries can represent intensities; in data analysis, low-rank structure can summarize coordinated variation.\n\nLarge singular values do not automatically identify scientifically important causes, and a small singular value can still carry meaningful information. Data scaling and the chosen error measure matter. Your goal here is to read and verify a supplied SVD, connect it to rank and geometry, and explain what truncation keeps and loses. Computing a large SVD from scratch is beyond this introductory sequence.\n\n### What a low-rank approximation promises\n\nFor singular values five, three, and one, retaining two directions discards squared error one; retaining only one discards squared error ten. The Frobenius errors are respectively one and square root of ten. Squared error and error norm are different quantities. Keeping zero directions gives the zero matrix and loses all three contributions; keeping every positive direction recovers A exactly.\n\nThe positive smallest singular value of a square invertible matrix controls its weakest stretch. Its reciprocal is the largest stretch of the inverse, so a very small value can amplify measurement errors when solving backward. For diagonal entries one and one hundredth, changing the second output by one hundredth changes the recovered second input by one. This is a concrete sensitivity calculation, not a reason to declare every small direction meaningless.\n\nThe roadmap's goal is conceptual SVD: interpret factor shapes, reconstruct small supplied decompositions, connect rank and null spaces, and quantify truncation error. It does not promise algorithms for large matrices or a proof of the approximation theorem. Those boundaries should remain explicit while the examples and practice within them are substantial.",
    "example": "A diagonal matrix has entries 6,3,1. Form a rank-one truncated SVD approximation and give its Frobenius error exactly.\n\nKeep only the entry six, giving diagonal entries (6,0,0). The discarded squared singular values sum to 9+1=10, so the Frobenius error is the square root of ten.",
    "confusion": "Approximation discards information. Optimality for a norm is not a claim of scientific importance, and tied values can make an optimum nonunique.",
    "lesson": "linear-algebra-svd",
    "section": "Approximation and responsible interpretation",
    "related": ["linear-algebra-svd-3"]
  },
  {
    "id": "la-symbol-real-coordinate-space",
    "kind": "symbol",
    "name": "\\mathbb{R}^n",
    "aliases": ["\\mathbb{R}^n", "Coordinate vector", "mathbb{R}^n", "real coordinate space"],
    "linkAliases": [],
    "quick": "The set of all ordered n-component real coordinate vectors; n is the number of coordinates.",
    "definition": "A real [coordinate vector](ref:linear-algebra-vectors-1) is an ordered list of real numbers. Its entries are called components. The notation $\\mathbb{R}^n$ means the set of all lists with exactly n real components; read it as “R to the n,” or “real n-dimensional coordinate space.” Here n is a positive integer, the dimension of this coordinate space. The superscript specifies the number of coordinates rather than a numerical power of a set.\n\nFor example, $u=(3,2)$ is a vector with two components. As a displacement, it means three units right and two units up. As data, it could mean three hours spent on one activity and two on another. The arithmetic is the same, but units and interpretation come from the application. Adding measurements with incompatible meanings can be mathematically possible and scientifically meaningless.\n\nA vector is not tied to a particular starting point. An arrow from the origin to (3,2) and an arrow from (1,1) to (4,3) represent the same displacement. A position is a point; its position vector is the displacement from the chosen origin. Keeping those ideas distinct helps when we later distinguish linear transformations from translations.\n\nWe can write the same components vertically as a column. Parentheses and square brackets are both common; a vertical arrangement does not change the [component](ref:la-term-component) values. Unless a lesson says otherwise, our matrix calculations use column vectors. In ordinary prose, coordinate tuples remain convenient shorthand.\n\n### Coordinates carry meaning\n\nSuppose a weather record is (12,5,-2), in the order temperature, wind speed, and overnight change in temperature. It has three components, but those components do not describe three spatial directions. A negative third component records a decrease; it does not invalidate the vector. Before doing arithmetic, write down the coordinate order and units. If another record uses a different order, reorder it before comparing corresponding components.\n\nFor a displacement, subtract start from finish in every coordinate. An arrow from (4,-2,1) to (1,3,1) has displacement (-3,5,0). Its zero third component means no change in that coordinate, not that the arrow is the zero vector. If only the starting point moves while the endpoint stays fixed, the displacement changes. It stays the same only when both endpoints are translated by the same amount.",
    "example": "An arrow starts at (2,-1) and ends at (5,3). Give its displacement vector. Explain whether translating both endpoints by the same amount changes that vector, and whether moving only the starting point would do so.\n\nThe displacement is (3,4): subtract the starting coordinates from the ending coordinates. Translating both endpoints by the same amount preserves these differences. Moving only the starting point while fixing the endpoint changes the displacement.",
    "confusion": "A vector's component order matters. Translating an arrow without changing its displacement does not create a different vector.",
    "lesson": "linear-algebra-vectors",
    "section": "Coordinates and vectors",
    "related": ["linear-algebra-vectors-1"]
  },
  {
    "id": "la-symbol-dot-product",
    "kind": "symbol",
    "name": "u\\cdot v",
    "aliases": ["u\\cdot v", "Dot product", "ucdot v", "dot product"],
    "linkAliases": [],
    "quick": "Multiply corresponding real components and add, producing one scalar.",
    "definition": "For real vectors of the same size, the [dot product](ref:linear-algebra-dot-products-1) multiplies corresponding components and adds the products. In two dimensions, $u\\cdot v=u_1v_1+u_2v_2$. Here u and v are vectors, while u sub one, u sub two, and the corresponding v symbols are their real components. The centered dot means this operation, not a new vector component.\n\nFor u=(2,-1) and v=(3,4), the dot product is two times three plus negative one times four, which is 2. The result is a scalar. This is different from multiplying two vectors component by component and keeping a list of products; that operation exists in other contexts but is not the dot product.\n\nThe same rule works with any finite number of components. Because ordinary multiplication commutes, exchanging the two vectors leaves their dot product unchanged. It also distributes across vector addition. These facts will let us expand squared distances without drawing every example.\n\n### Why distribution is useful\n\nFor u=(1,-2,2), the dot product with itself is 1+4+4=9. Every square is nonnegative, so a real vector has zero dot product with itself only when all its components vanish. This observation will explain why squared length detects zero.\n\nTo expand the dot product of u+v with u-v, distribute in both positions. The four terms are u dot u, negative u dot v, v dot u, and negative v dot v. The middle terms cancel by symmetry. The result is the squared length of u minus the squared length of v. Explaining that cancellation is more useful than memorizing a separate vector identity.",
    "example": "Compute the dot product of (1,-2,3) and (4,0,-1). Is the result a scalar or a vector?\n\nThe component products are 4, 0, and -3; their sum is 1. The result is a scalar, not a list of three products.",
    "confusion": "This is not a list of componentwise products. A zero dot product does not mean either input is zero.",
    "lesson": "linear-algebra-dot-products",
    "section": "A scalar from two vectors",
    "related": ["linear-algebra-dot-products-1"]
  },
  {
    "id": "la-symbol-norm",
    "kind": "symbol",
    "name": "\\|u\\|",
    "aliases": ["\\|u\\|", "Euclidean norm", "|u|", "norm"],
    "linkAliases": [],
    "quick": "The square root of the sum of squared components: a vector's usual length.",
    "definition": "The [Euclidean norm](ref:linear-algebra-dot-products-2) is the usual geometric length of a vector. We write $\\|u\\|=\\sqrt{u\\cdot u}$, read “the norm of u equals the square root of u dot u.” The double vertical bars mean length here. Squaring and adding the components gives the familiar Pythagorean calculation, extended to more coordinates.\n\nFor u=(3,4), the squared length is 25 and the length is 5. Length is nonnegative, and it is zero only for the zero vector. Distance between two points is the norm of their difference, because that difference is the displacement connecting them.\n\nA [unit vector](ref:la-term-unit-vector) has length one. Normalizing a nonzero vector means dividing it by its norm. Thus (3/5,4/5) is a unit vector pointing in the direction of (3,4). Division here scales every component by the reciprocal of the length. The zero vector cannot be normalized: division by its zero length is undefined, and there is no direction to preserve.\n\n### Normalization changes size, not direction\n\nFor v=(-2,1,2), the sum of squared components is 9, so its length is 3 and its [normalization](ref:la-term-normalization) is (-2/3,1/3,2/3). The negative coordinate remains negative. A unit vector is not a vector with every entry equal to one; in fact (1,1,1) has length square root of three.\n\n$\n\\frac{1}{3}\\begin{pmatrix}-2\\\\1\\\\2\\end{pmatrix}=\\begin{pmatrix}-2/3\\\\1/3\\\\2/3\\end{pmatrix}\n$\n\nScaling a vector by a real number c multiplies its squared length by c squared, and therefore multiplies its length by the absolute value of c. The absolute value matters when c is negative. Likewise the distance between points depends on the difference vector, so translating both points by the same displacement does not change their distance.",
    "example": "Find the norm of (6,8) and a unit vector in its direction. Explain why your normalized vector has length one.\n\nThe norm is the square root of 36+64, or 10. Dividing gives (3/5,4/5). The sum of its squared components is 9/25+16/25=1, so its norm is one.",
    "confusion": "Norm is nonnegative. The zero vector has no direction and cannot be normalized.",
    "lesson": "linear-algebra-dot-products",
    "section": "Length and unit vectors",
    "related": ["linear-algebra-dot-products-2"]
  },
  {
    "id": "la-symbol-transpose",
    "kind": "symbol",
    "name": "A^T",
    "aliases": ["A^T", "Matrix and transpose", "transpose"],
    "linkAliases": [],
    "quick": "A transpose: exchange the rows and columns of A.",
    "definition": "An m by n matrix has m rows and n columns, where m and n are positive integers. An entry is one number in the array. We write $A_{ij}$ for the entry in row i and column j of matrix A, numbering rows and columns from one. The first subscript is always the row in this convention.\n\nFor example, $A=\\begin{pmatrix}1&2\\\\3&4\\end{pmatrix}$ has two rows and two columns, and its entry in row two, column one is 3. A square matrix has the same number of rows and columns. A column vector is an n by one matrix when we use matrix notation.\n\nThe [transpose](ref:la-term-transpose) exchanges rows and columns. The notation $A^T$ is read “A transpose”; T is a label for this operation, not an exponent. The example's transpose has rows (1,3) and (2,4). Transposing an m by n matrix produces an n by m matrix. Adding matrices requires equal shapes and adds corresponding entries; scalar multiplication scales every entry.\n\n### Rectangular arrays are ordinary matrices\n\nConsider a table with rows (1,2,0) and (-1,3,4). It has two rows and three columns, so it can multiply a three-component input. Its transpose has rows (1,-1), (2,3), and (0,4). Transposing twice restores the original table. A row and a column can contain the same entries but have different shapes, which changes what they can multiply.\n\nA matrix is symmetric when it equals its transpose. This requires a square shape and matching entries across the main diagonal. For example, rows (2,-1),(-1,5) give a [symmetric matrix](ref:la-term-symmetric-matrix); a square matrix with rows (2,0),(-1,5) is not symmetric.",
    "example": "A matrix has rows (1,2,3) and (4,5,6). State its shape, its entry in row two column three, and the rows of its transpose.\n\nIts shape is two by three, and the specified entry is 6. Its transpose is three by two with rows (1,4), (2,5), and (3,6).",
    "confusion": "The first entry index names a row. A superscript T denotes transpose, not a numerical power.",
    "lesson": "linear-algebra-matrices",
    "section": "Shape, entries, and transpose",
    "related": ["linear-algebra-matrices-1"]
  },
  {
    "id": "la-symbol-span",
    "kind": "symbol",
    "name": "\\operatorname{span}\\{u,v\\}",
    "aliases": ["\\operatorname{span}\\{u,v\\}", "Span", "operatorname{span}{u,v}", "span"],
    "linkAliases": [],
    "quick": "All real linear combinations of a supplied collection of vectors.",
    "definition": "The span of a collection of vectors is the set of all their linear combinations, using arbitrary real coefficients. We write $\\operatorname{span}\\{u,v\\}$ for the span of u and v, read “the span of u and v.” Braces specify the supplied collection, while span denotes all outputs obtainable from it. The vectors in this lesson belong to a fixed real coordinate space.\n\nThe span of a single nonzero vector is a line through the origin. The span of (1,0) and (0,1) is the whole coordinate plane. The span of (1,1) and (2,2) is only the diagonal line through (1,1): the second vector gives no direction that the first could not already produce. The zero vector belongs to every span, because all coefficients can be zero.\n\nTo test whether b lies in a given span, place the supplied vectors in the columns of a matrix A and solve Ax=b. A solution provides coefficients that build b. An inconsistent system proves that no such combination exists. This turns a geometric reachability question into a calculation you already know.\n\n### Describing an entire set\n\nThe span of (1,0,2) and (0,1,-1) consists of vectors (a,b,2a-b) for arbitrary real a,b. This description gives both a way to build members and a membership test: the third coordinate must equal twice the first minus the second. The vector (2,3,1) belongs, while (2,3,0) does not. A few plotted arrows cannot replace this all-coefficients argument.\n\nThe span of an empty collection is defined to be the zero-only subspace: the empty sum is zero. Adding the zero vector to a spanning collection changes nothing, and adding any vector already in its span changes nothing. To show two collections have equal spans, show each supplied vector in either collection can be built from the other collection.",
    "example": "Does (3,4) lie in the span of u=(1,0) and v=(1,2)? Find coefficients or prove none exist.\n\nYes. The second coordinate forces the coefficient of v to be 2. The first then forces the coefficient of u to be 1, so u+2v=(3,4).",
    "confusion": "The span is a set of outputs, not the original short list of building blocks. Always specify the ambient space when claiming full coverage.",
    "lesson": "linear-algebra-span",
    "section": "Span is everything you can build",
    "related": ["linear-algebra-span-1"]
  },
  {
    "id": "la-symbol-rank-nullity",
    "kind": "symbol",
    "name": "\\operatorname{rank}(A)+\\operatorname{nullity}(A)=n",
    "aliases": [
      "\\operatorname{rank}(A)+\\operatorname{nullity}(A)=n",
      "Rank and nullity",
      "operatorname{rank}(A)+operatorname{nullity}(A)=n",
      "rank nullity"
    ],
    "linkAliases": [],
    "quick": "Rank plus nullity equals n, the number of input columns of A.",
    "definition": "The [rank](ref:la-term-rank) of a matrix is the dimension of its column space, the number of independent output directions its columns supply. Elimination finds it by counting pivot columns. To obtain a basis of the column space, take the pivot-indexed columns of the original matrix, not the changed columns of its row-reduced form. Row operations preserve dependencies among columns but can change the column space itself.\n\nThe [nullity](ref:la-term-nullity) is the dimension of the null space, or equivalently the number of free variables in the homogeneous system. For a matrix with n columns, $\\operatorname{rank}(A)+\\operatorname{nullity}(A)=n$. Read this as “rank of A plus nullity of A equals n.” Every input variable is either a pivot variable or a free variable, which explains the count.\n\nFor the matrix with columns (1,0), (0,1), and (1,1), rank is two and nullity is one. The third column adds no output direction, and coefficients (-1,-1,1) give a null-space direction. The matrix reaches every two-component output but loses some information about the three-component input.\n\n### Finding both spaces in one reduction\n\nTake A with rows (1,2,0),(0,0,1). The pivot columns are first and third, so a column-space basis is (1,0),(0,1). For the null space, the equations are x+2y=0 and z=0. Letting y=t gives all multiples of (-2,1,0). Rank is two, nullity one, and their sum is the three input coordinates.\n\n$\n\\begin{pmatrix}1&2&0\\\\0&0&1\\end{pmatrix}\\begin{pmatrix}-2\\\\1\\\\0\\end{pmatrix}=\\begin{pmatrix}0\\\\0\\end{pmatrix}\n$\n\nThe nonzero rows of a row-echelon form form a basis of the row space. Their distinct leading positions make them independent, and reversible row operations preserve their span. Consequently row-space dimension equals column-space dimension: both count pivots. For an m-by-n matrix, rank is at most the smaller of m and n. A full-column-rank matrix has rank n; a full-row-rank matrix has rank m.",
    "example": "A matrix has five columns and three pivots. Find its rank and nullity. What do the free variables describe?\n\nRank is three and nullity is two. The two free variables parameterize the homogeneous solutions, describing input freedom that produces no output change.",
    "confusion": "Their sum is the number of columns. Select a column-space basis from original columns at pivot indices.",
    "lesson": "linear-algebra-bases",
    "section": "Rank and nullity",
    "related": ["linear-algebra-bases-2"]
  },
  {
    "id": "la-symbol-inverse",
    "kind": "symbol",
    "name": "A^{-1}",
    "aliases": ["A^{-1}", "Matrix inverse", "inverse"],
    "linkAliases": [],
    "quick": "A matrix that undoes a square matrix, giving identity when multiplied in either order.",
    "definition": "A square matrix A is invertible if there is a matrix written $A^{-1}$ such that multiplying in either order gives the identity. The superscript negative one means matrix inverse here. It does not mean taking the reciprocal of each entry.\n\nThe inverse undoes A. If Ax=b, multiply on the left by the inverse to obtain x equal to A inverse times b. An n by n matrix is invertible exactly when its rank is n: elimination then leaves no free variables and permits every right-hand side. Its columns consequently form a basis of the full output space.\n\nThe diagonal matrix with diagonal entries 2 and 3 is inverted by the diagonal matrix with entries one half and one third. More generally, for a two by two matrix with rows (a,b),(c,d), the determinant is the scalar $ad-bc$. If it is nonzero, the inverse has rows (d,-b),(-c,a), all divided by that determinant. Multiplying the proposed inverse by the original verifies the formula. If the determinant is zero, elimination gives deficient rank and there is no inverse.\n\n### What a determinant measures in the plane\n\nFor columns (a,c) and (b,d), the absolute value of ad-bc is the area of the parallelogram they form. Its sign records orientation relative to the standard axes. Zero means the parallelogram collapses into a line or point, precisely the loss of an independent direction that prevents a two-by-two inverse. Area scaling describes this [two-dimensional determinant](ref:la-term-determinant-plane); it is not the length scaling of every vector.\n\nFor rows (2,1),(1,1), the determinant is one and the inverse has rows (1,-1),(-1,2). The first product row is (2-1,-2+2)=(1,0), and the second is (1-1,-1+2)=(0,1). This verifies the proposed inverse directly. The inverse of a product AB of invertible square matrices is B inverse times A inverse: undo the last action first.",
    "example": "Find the inverse of the matrix with rows (1,2),(0,1), and verify it by multiplication.\n\nThe inverse has rows (1,-2),(0,1). Multiplying in either order yields diagonal entries one and off-diagonal entries zero, the identity.",
    "confusion": "The inverse is not formed by reciprocating entries. A zero two-by-two determinant prevents invertibility.",
    "lesson": "linear-algebra-bases",
    "section": "Invertible matrices",
    "related": ["linear-algebra-bases-3"]
  },
  {
    "id": "la-symbol-projection",
    "kind": "symbol",
    "name": "p=\\frac{b\\cdot u}{u\\cdot u}u",
    "aliases": [
      "p=\\frac{b\\cdot u}{u\\cdot u}u",
      "Orthogonal projection",
      "p=frac{bcdot u}{ucdot u}u",
      "projection"
    ],
    "linkAliases": [],
    "quick": "The closest vector in a subspace; its residual is perpendicular to that subspace.",
    "definition": "Let u be a nonzero real vector and b a target in the same coordinate space. The [projection](ref:linear-algebra-projections-1) p of b onto the line spanned by u is $p=\\frac{b\\cdot u}{u\\cdot u}u$. Read this as “b dot u divided by u dot u, times u.” The denominator is positive because u is nonzero. The scalar coefficient tells us how far along the line to go.\n\nThe [residual](ref:la-term-residual) r is the difference b-p, pointing from the approximation to the target. Substitution into its dot product with u gives zero, so the residual is perpendicular to the permitted line. For b=(3,1) and u=(1,0), the projection is (3,0) and the residual (0,1).\n\nWhy does perpendicular mean closest? Any other point q on the line differs from p by a vector along the line, perpendicular to the residual. The Pythagorean theorem says the squared distance from b to q equals the squared residual length plus the squared distance from p to q. The extra term is nonnegative and vanishes only at p. This proves minimal distance rather than merely suggesting it from a picture.\n\n\n\n### A non-axis projection with exact arithmetic\n\nLet b=(3,0) and u=(1,2). Their dot product is three and u dot u is five, so the coefficient is 3/5. The projected vector is (3/5,6/5), and the residual b-p is (12/5,-6/5). Its dot product with u is 12/5-12/5=0. The residual's squared norm is 36/5. Reporting only the coefficient 3/5 would not report the point on the line.\n\nReplacing u by any nonzero scalar multiple gives the same line and the same projected vector. The coefficient changes to compensate for the changed building block. If b already lies on the line, its residual is zero. If b is perpendicular to the line, the projection is zero.",
    "example": "Project b=(4,2) onto the line spanned by u=(1,1). Find the residual and check its perpendicularity to u.\n\nThe coefficient is (4+2)/(1+1)=3. The projection is (3,3), and the residual is (1,-1). Its dot product with u is 1-1=0.",
    "confusion": "A nonzero line direction is required in the line formula. A nonzero residual is expected for a target outside the subspace.",
    "lesson": "linear-algebra-projections",
    "section": "Projecting onto a line",
    "related": ["linear-algebra-projections-1"]
  },
  {
    "id": "la-symbol-eigenvalue",
    "kind": "symbol",
    "name": "Av=\\lambda v",
    "aliases": ["Av=\\lambda v", "Eigenvalue and eigenvector", "Av=lambda v", "eigenvalue"],
    "linkAliases": [],
    "quick": "A nonzero vector sent to a scalar multiple of itself is an eigenvector; that scalar is its eigenvalue.",
    "definition": "For a square matrix A, a nonzero vector v is an [eigenvector](ref:linear-algebra-eigenvalues-1) if $Av=\\lambda v$ for some scalar lambda, its eigenvalue. Read the equation as “A times v equals lambda times v.” The Greek letter lambda names a number; v must be nonzero, since zero would satisfy the equation for every number and tell us nothing.\n\nFor the diagonal matrix with entries 2 and 3, the first standard vector is scaled by two and the second by three. Those are eigenvectors with different eigenvalues. The vector (1,1) is sent to (2,3), so it is not an eigenvector: no single scalar multiplies both original components to give the result.\n\nA negative eigenvalue reverses the vector as well as changing its length. A zero eigenvalue sends a nonzero vector to zero, so a matrix with zero as an eigenvalue is not invertible. Multiplying an eigenvector by any nonzero scalar produces another eigenvector for the same eigenvalue. An eigendirection therefore has many representative vectors.\n\n### Verifying a candidate before solving a polynomial\n\nLet A have rows (2,1),(1,2). It sends (1,1) to (3,3), so that direction has eigenvalue three. It sends (1,-1) to (1,-1), so that direction has eigenvalue one. The vectors are independent and perpendicular. For (2,0), the output (4,2) is not a scalar multiple, so this particular vector is not an eigenvector even though A has an eigenbasis.\n\nThe word eigen comes from German usage for something's own or characteristic quantity. The terminology points to directions adapted to this particular transformation. There is no preferred eigenvector length: after finding a direction you may normalize it, but normalization cannot turn a non-eigenvector into an eigenvector.",
    "example": "A has diagonal entries 2 and -1, with zeros off the diagonal. Which of (1,0), (0,1), and (1,1) are eigenvectors? Give each corresponding eigenvalue.\n\nThe first vector is an eigenvector with eigenvalue 2, and the second with eigenvalue -1. The third goes to (2,-1), which is not a single scalar multiple of (1,1), so it is not an eigenvector.",
    "confusion": "Zero may be an eigenvalue but not an eigenvector. Negative eigenvalues reverse direction without giving negative lengths.",
    "lesson": "linear-algebra-eigenvalues",
    "section": "Directions that are only scaled",
    "related": ["linear-algebra-eigenvalues-1"]
  },
  {
    "id": "la-symbol-svd",
    "kind": "symbol",
    "name": "A=U\\Sigma V^T",
    "aliases": ["A=U\\Sigma V^T", "Singular value decomposition", "A=USigma V^T", "svd"],
    "linkAliases": [],
    "quick": "Factor a real matrix into orthogonal input/output coordinate changes and nonnegative diagonal scale factors.",
    "definition": "For an m by n real matrix A, a [singular value decomposition](ref:linear-algebra-svd-1) is $A=U\\Sigma V^T$. Here U is an m by m [orthogonal matrix](ref:la-term-orthogonal-matrix), V is an n by n orthogonal matrix, and Sigma is an m by n rectangular diagonal matrix. Orthogonal matrix means a square matrix whose columns form an orthonormal basis; its transpose is its inverse. Sigma has nonnegative numbers on its diagonal and zero elsewhere.\n\nThe diagonal numbers are singular values, usually listed from largest to smallest. The columns of V are right singular vectors in the input space; columns of U are left singular vectors in the output space. The Greek capital Sigma names this matrix here, not a summation operation. We use the full SVD convention in this lesson; software may return smaller versions that omit unused directions.\n\nRead the product from right to left. V transpose expresses the input in perpendicular coordinates, Sigma scales those coordinates and may discard some, and U expresses the result in output coordinates. The orthogonal factors preserve Euclidean length; stretching and information loss occur in Sigma. Orthogonal factors can include reflections, so calling them only “rotations” would be too restrictive.\n\n### Rectangular factors need a dimension check\n\nAn m-by-n matrix has only min(m,n) diagonal positions in its rectangular Sigma. Here min means the smaller of the two numbers. If there are more inputs than outputs, some input basis directions have no diagonal position at all and are necessarily erased. For example, the two-by-three matrix with rows (3,0,0),(0,2,0) has listed singular values three and two but still has the third standard vector in its null space. Always compute nullity as number of columns minus rank, not just by counting zeros in a shortened singular-value list.\n\nFor a tall matrix, the extra rows of Sigma are zero and describe unused output directions. Orthonormal changes of coordinates do not alter vector length within their own spaces; the rectangular diagonal factor is the step that changes dimension. This is why the shapes of all three factors matter as much as their entries.",
    "example": "For a three by two matrix, state the shapes of U, Sigma, and V in the full SVD. Explain which factor contains the length scale factors.\n\nU is three by three, Sigma is three by two, and V is two by two. Sigma contains nonnegative singular values on its diagonal; the orthogonal U and V factors preserve length.",
    "confusion": "The full SVD uses square U and V and rectangular Sigma. Orthogonal factors can be reflections as well as rotations.",
    "lesson": "linear-algebra-svd",
    "section": "Perpendicular directions and scale factors",
    "related": ["linear-algebra-svd-1"]
  },
  {
    "id": "la-term-component",
    "kind": "term",
    "name": "Component",
    "aliases": ["Component"],
    "linkAliases": [],
    "quick": "One entry in an ordered coordinate vector.",
    "definition": "One entry in an ordered coordinate vector.\n\nA real [coordinate vector](ref:linear-algebra-vectors-1) is an ordered list of real numbers. Its entries are called components. The notation $\\mathbb{R}^n$ means the set of all lists with exactly n real components; read it as “R to the n,” or “real n-dimensional coordinate space.” Here n is a positive integer, the dimension of this coordinate space. The superscript specifies the number of coordinates rather than a numerical power of a set.",
    "example": "In (2,-3,0), the second component is -3.",
    "confusion": "A zero component does not mean the whole vector is zero.",
    "lesson": "linear-algebra-vectors",
    "section": "Coordinates and vectors",
    "related": ["linear-algebra-vectors-1"]
  },
  {
    "id": "la-term-scalar",
    "kind": "term",
    "name": "Scalar",
    "aliases": ["Scalar"],
    "linkAliases": [],
    "quick": "A real number used to scale every component of a vector.",
    "definition": "A real number used to scale every component of a vector.\n\nA scalar is an ordinary number used to multiply a vector. [Scalar multiplication](ref:linear-algebra-vectors-2) multiplies every component: $2u=(6,4)$. For a nonzero vector, a positive scalar stretches or shrinks its arrow while preserving its direction; a negative scalar reverses it as well. Multiplication by zero produces the zero vector, whose components are all zero and which has no direction.",
    "example": "Multiplying (1,-2) by scalar three gives (3,-6).",
    "confusion": "The scalar is a number; the scaled result is still a vector.",
    "lesson": "linear-algebra-vectors",
    "section": "Adding and scaling",
    "related": ["linear-algebra-vectors-2"]
  },
  {
    "id": "la-term-unit-vector",
    "kind": "term",
    "name": "Unit vector",
    "aliases": ["Unit vector"],
    "linkAliases": [],
    "quick": "A vector of Euclidean length one.",
    "definition": "A vector of Euclidean length one.\n\nA unit vector has length one. Normalizing a nonzero vector means dividing it by its norm. Thus (3/5,4/5) is a unit vector pointing in the direction of (3,4). Division here scales every component by the reciprocal of the length. The zero vector cannot be normalized: division by its zero length is undefined, and there is no direction to preserve.",
    "example": "The vector (3/5,4/5) has squared length one.",
    "confusion": "Unit length does not mean every component equals one.",
    "lesson": "linear-algebra-dot-products",
    "section": "Length and unit vectors",
    "related": ["linear-algebra-dot-products-2"]
  },
  {
    "id": "la-term-normalization",
    "kind": "term",
    "name": "Normalization",
    "aliases": ["Normalization"],
    "linkAliases": [],
    "quick": "Dividing a nonzero vector by its norm to preserve direction and obtain unit length.",
    "definition": "Dividing a nonzero vector by its norm to preserve direction and obtain unit length.\n\nA unit vector has length one. Normalizing a nonzero vector means dividing it by its norm. Thus (3/5,4/5) is a unit vector pointing in the direction of (3,4). Division here scales every component by the reciprocal of the length. The zero vector cannot be normalized: division by its zero length is undefined, and there is no direction to preserve.",
    "example": "Normalizing (0,-2) gives (0,-1).",
    "confusion": "The zero vector cannot be normalized by this rule.",
    "lesson": "linear-algebra-dot-products",
    "section": "Length and unit vectors",
    "related": ["linear-algebra-dot-products-2"]
  },
  {
    "id": "la-term-transpose",
    "kind": "term",
    "name": "Transpose",
    "aliases": ["Transpose"],
    "linkAliases": [],
    "quick": "The matrix obtained by exchanging rows and columns.",
    "definition": "The matrix obtained by exchanging rows and columns.\n\nThe transpose exchanges rows and columns. The notation $A^T$ is read “A transpose”; T is a label for this operation, not an exponent. The example's transpose has rows (1,3) and (2,4). Transposing an m by n matrix produces an n by m matrix. Adding matrices requires equal shapes and adds corresponding entries; scalar multiplication scales every entry.",
    "example": "Rows (1,2,3),(4,5,6) transpose to rows (1,4),(2,5),(3,6).",
    "confusion": "A superscript T names an operation, not a scalar exponent.",
    "lesson": "linear-algebra-matrices",
    "section": "Shape, entries, and transpose",
    "related": ["linear-algebra-matrices-1"]
  },
  {
    "id": "la-term-symmetric-matrix",
    "kind": "term",
    "name": "Symmetric matrix",
    "aliases": ["Symmetric matrix"],
    "linkAliases": [],
    "quick": "A square real matrix equal to its transpose.",
    "definition": "A square real matrix equal to its transpose.\n\nA matrix is symmetric when it equals its transpose. This requires a square shape and matching entries across the main diagonal. For example, rows (2,-1),(-1,5) give a symmetric matrix; a square matrix with rows (2,0),(-1,5) is not symmetric.",
    "example": "Rows (2,1),(1,3) are symmetric.",
    "confusion": "Being square alone does not imply symmetry.",
    "lesson": "linear-algebra-matrices",
    "section": "Shape, entries, and transpose",
    "related": ["linear-algebra-matrices-1"]
  },
  {
    "id": "la-term-identity-matrix",
    "kind": "term",
    "name": "Identity matrix",
    "aliases": ["Identity matrix"],
    "linkAliases": [],
    "quick": "A square matrix with ones on the main diagonal and zeros elsewhere, leaving compatible vectors unchanged.",
    "definition": "A square matrix with ones on the main diagonal and zeros elsewhere, leaving compatible vectors unchanged.\n\nAn [identity matrix](ref:linear-algebra-matrices-2) leaves every compatible vector unchanged. In two dimensions it has rows (1,0) and (0,1). It is written $I_2$, with the subscript giving its size. More generally, an identity matrix has ones on its main diagonal, the entries whose row and column numbers agree, and zeros elsewhere.",
    "example": "The two-dimensional identity sends (a,b) to (a,b).",
    "confusion": "The size must match the input or output side of a multiplication.",
    "lesson": "linear-algebra-matrices",
    "section": "A matrix acting on a vector",
    "related": ["linear-algebra-matrices-2"]
  },
  {
    "id": "la-term-augmented-matrix",
    "kind": "term",
    "name": "Augmented matrix",
    "aliases": ["Augmented matrix"],
    "linkAliases": [],
    "quick": "A coefficient matrix with its right-hand-side column appended.",
    "definition": "A coefficient matrix with its right-hand-side column appended.\n\nAn [augmented matrix](ref:linear-algebra-systems-1) places the right-hand-side column beside the coefficient columns, usually separated visually by a bar. It records the same system compactly. Keep the variable order fixed: if the columns mean x then y, they must keep that meaning through every row operation.",
    "example": "The equation x+2y=3 has augmented row (1,2,3).",
    "confusion": "The last column is not an additional variable.",
    "lesson": "linear-algebra-systems",
    "section": "Constraints and augmented matrices",
    "related": ["linear-algebra-systems-1"]
  },
  {
    "id": "la-term-echelon-form",
    "kind": "term",
    "name": "Row echelon form",
    "aliases": ["Row echelon form"],
    "linkAliases": [],
    "quick": "Zero rows lie below nonzero rows; successive leading entries move right and have zeros beneath them.",
    "definition": "Zero rows lie below nonzero rows; successive leading entries move right and have zeros beneath them.\n\nGaussian elimination uses these operations to remove coefficients below successive leading entries. A [pivot](ref:linear-algebra-systems-2) is a leading nonzero entry chosen to eliminate other entries in its column. In row echelon form, all zero rows are at the bottom, each nonzero row's leading entry is farther right than the one above, and entries below each leading entry are zero.",
    "example": "Rows (1,2,0),(0,0,1) are in echelon form.",
    "confusion": "A leading entry need not be one until reduced echelon requirements are imposed.",
    "lesson": "linear-algebra-systems",
    "section": "Elimination preserves solutions",
    "related": ["linear-algebra-systems-2"]
  },
  {
    "id": "la-term-reduced-echelon-form",
    "kind": "term",
    "name": "Reduced row echelon form",
    "aliases": ["Reduced row echelon form"],
    "linkAliases": [],
    "quick": "Echelon form whose leading entries are one and the only nonzero entries in their columns.",
    "definition": "Echelon form whose leading entries are one and the only nonzero entries in their columns.\n\nFor the system above, replace the second row by the second row minus twice the first. The second equation becomes negative three y equals negative six. Solve it first, then substitute upward; this is back substitution. Reduced row echelon form goes further: each leading entry is one, and all other entries in its column are zero. Either form can be used to solve a system.",
    "example": "Rows (1,0,2),(0,1,-1) satisfy the reduced requirements.",
    "confusion": "Reduced echelon form may still contain non-pivot columns and free variables.",
    "lesson": "linear-algebra-systems",
    "section": "Elimination preserves solutions",
    "related": ["linear-algebra-systems-2"]
  },
  {
    "id": "la-term-consistent-system",
    "kind": "term",
    "name": "Consistent system",
    "aliases": ["Consistent system"],
    "linkAliases": [],
    "quick": "A system with at least one simultaneous solution.",
    "definition": "A system with at least one simultaneous solution.\n\nA system is consistent if it has at least one solution. An equation such as zero equals one is impossible, so an augmented row with all zero coefficient entries and a nonzero last entry proves inconsistency. A row representing zero equals zero adds no restriction and does not itself indicate failure.",
    "example": "The equations x+y=1 and 2x+2y=2 are consistent with infinitely many solutions.",
    "confusion": "Consistent does not mean uniquely solvable.",
    "lesson": "linear-algebra-systems",
    "section": "No solution or a family of solutions",
    "related": ["linear-algebra-systems-3"]
  },
  {
    "id": "la-term-parameter",
    "kind": "term",
    "name": "Parameter",
    "aliases": ["Parameter"],
    "linkAliases": [],
    "quick": "A symbol allowed to range over a stated domain to describe a family of solutions or objects.",
    "definition": "A symbol allowed to range over a stated domain to describe a family of solutions or objects.\n\nFor example, x+y=3 and twice x plus twice y equals six describe the same line. One variable may be chosen freely. Writing $y=t,\\quad x=3-t$ describes every solution, with t an arbitrary real parameter. A parameter is a symbol that ranges over allowed values to describe a family of objects.",
    "example": "Writing y=t and x=3-t with real t describes a whole line of solutions.",
    "confusion": "Different free variables generally need independent parameters.",
    "lesson": "linear-algebra-systems",
    "section": "No solution or a family of solutions",
    "related": ["linear-algebra-systems-3"]
  },
  {
    "id": "la-term-null-space",
    "kind": "term",
    "name": "Null space",
    "aliases": ["Null space"],
    "linkAliases": [],
    "quick": "The subspace of input vectors sent to zero by a matrix.",
    "definition": "The subspace of input vectors sent to zero by a matrix.\n\nThe null space of A is the set of solutions of Ax=0. It is a subspace of the input space: zero solves the equation, sums of solutions solve it, and scalar multiples solve it. The column space of A is the span of its columns, hence the set of possible outputs. It is a subspace of the output space. These spaces need not have the same ambient dimension when A is rectangular.",
    "example": "For the row matrix (1,2), the null space consists of all multiples of (-2,1).",
    "confusion": "Null vectors have one component per input column, not one per output row.",
    "lesson": "linear-algebra-span",
    "section": "Subspaces and the origin",
    "related": ["linear-algebra-span-3"]
  },
  {
    "id": "la-term-column-space",
    "kind": "term",
    "name": "Column space",
    "aliases": ["Column space"],
    "linkAliases": [],
    "quick": "The span of the columns of a matrix, equivalently all outputs the matrix reaches.",
    "definition": "The span of the columns of a matrix, equivalently all outputs the matrix reaches.\n\nThe null space of A is the set of solutions of Ax=0. It is a subspace of the input space: zero solves the equation, sums of solutions solve it, and scalar multiples solve it. The column space of A is the span of its columns, hence the set of possible outputs. It is a subspace of the output space. These spaces need not have the same ambient dimension when A is rectangular.",
    "example": "A matrix with columns (1,0),(2,0) reaches exactly the horizontal axis.",
    "confusion": "Row reduction can change the actual column space; use original pivot columns for its basis.",
    "lesson": "linear-algebra-span",
    "section": "Subspaces and the origin",
    "related": ["linear-algebra-span-3"]
  },
  {
    "id": "la-term-row-space",
    "kind": "term",
    "name": "Row space",
    "aliases": ["Row space"],
    "linkAliases": [],
    "quick": "The span of the matrix rows, considered as vectors in input coordinate space.",
    "definition": "The span of the matrix rows, considered as vectors in input coordinate space.\n\nThe row space of a matrix is the span of its rows, viewed as vectors with one component per column. It belongs to the input coordinate space, unlike the column space. Row operations preserve the row space because each new row is a combination of old rows and the operations are reversible. They need not preserve the actual column space.",
    "example": "Rows (1,2),(2,4) span the line generated by (1,2).",
    "confusion": "The row and column spaces can live in different coordinate spaces.",
    "lesson": "linear-algebra-span",
    "section": "Subspaces and the origin",
    "related": ["linear-algebra-span-3"]
  },
  {
    "id": "la-term-dimension",
    "kind": "term",
    "name": "Dimension",
    "aliases": ["Dimension"],
    "linkAliases": [],
    "quick": "The number of vectors in a basis of a finite-dimensional space.",
    "definition": "The number of vectors in a basis of a finite-dimensional space.\n\nThe dimension of a subspace is the number of vectors in a basis. A fundamental finite-dimensional result says that every basis of the same space has the same size. One way to understand why is an exchange argument: independent vectors can replace members of a spanning list one at a time, and there cannot be more independent replacements than available members. Applying this in both directions to two bases forces equal sizes.",
    "example": "A plane through the origin has dimension two even inside three-dimensional ambient space.",
    "confusion": "Count independent directions, not the number of entries in a displayed matrix.",
    "lesson": "linear-algebra-bases",
    "section": "A basis gives unique coordinates",
    "related": ["linear-algebra-bases-1"]
  },
  {
    "id": "la-term-rank",
    "kind": "term",
    "name": "Rank",
    "aliases": ["Rank"],
    "linkAliases": [],
    "quick": "The dimension of the column space, also the row-space dimension and pivot count.",
    "definition": "The dimension of the column space, also the row-space dimension and pivot count.\n\nThe rank of a matrix is the dimension of its column space, the number of independent output directions its columns supply. Elimination finds it by counting pivot columns. To obtain a basis of the column space, take the pivot-indexed columns of the original matrix, not the changed columns of its row-reduced form. Row operations preserve dependencies among columns but can change the column space itself.",
    "example": "Rows (1,2,0),(0,0,1) have two pivots and rank two.",
    "confusion": "Matrix size gives only an upper bound on rank.",
    "lesson": "linear-algebra-bases",
    "section": "Rank and nullity",
    "related": ["linear-algebra-bases-2"]
  },
  {
    "id": "la-term-nullity",
    "kind": "term",
    "name": "Nullity",
    "aliases": ["Nullity"],
    "linkAliases": [],
    "quick": "The dimension of a matrix null space, equal to its number of columns minus rank.",
    "definition": "The dimension of a matrix null space, equal to its number of columns minus rank.\n\nThe nullity is the dimension of the null space, or equivalently the number of free variables in the homogeneous system. For a matrix with n columns, $\\operatorname{rank}(A)+\\operatorname{nullity}(A)=n$. Read this as “rank of A plus nullity of A equals n.” Every input variable is either a pivot variable or a free variable, which explains the count.",
    "example": "A matrix with five columns and rank three has nullity two.",
    "confusion": "Subtract rank from the number of columns, not the number of rows.",
    "lesson": "linear-algebra-bases",
    "section": "Rank and nullity",
    "related": ["linear-algebra-bases-2"]
  },
  {
    "id": "la-term-determinant-plane",
    "kind": "term",
    "name": "Two-dimensional determinant",
    "aliases": ["Two-dimensional determinant"],
    "linkAliases": [],
    "quick": "For rows (a,b),(c,d), the scalar ad-bc; its magnitude gives area scaling and zero detects singularity.",
    "definition": "For rows (a,b),(c,d), the scalar ad-bc; its magnitude gives area scaling and zero detects singularity.\n\nThe diagonal matrix with diagonal entries 2 and 3 is inverted by the diagonal matrix with entries one half and one third. More generally, for a two by two matrix with rows (a,b),(c,d), the determinant is the scalar $ad-bc$. If it is nonzero, the inverse has rows (d,-b),(-c,a), all divided by that determinant. Multiplying the proposed inverse by the original verifies the formula. If the determinant is zero, elimination gives deficient rank and there is no inverse.",
    "example": "Rows (2,1),(1,1) give determinant 2-1=1.",
    "confusion": "Area scaling is not uniform length scaling. This entry uses the two-by-two formula.",
    "lesson": "linear-algebra-bases",
    "section": "Invertible matrices",
    "related": ["linear-algebra-bases-3"]
  },
  {
    "id": "la-term-kernel",
    "kind": "term",
    "name": "Kernel",
    "aliases": ["Kernel"],
    "linkAliases": [],
    "quick": "For a linear map, the subspace of inputs whose output is zero.",
    "definition": "For a linear map, the subspace of inputs whose output is zero.\n\nThe kernel of a linear transformation is the set of inputs sent to zero. The image is the set of outputs it actually reaches. For a matrix rule, these are respectively the null space and column space already studied. The domain specifies permitted inputs, while the codomain specifies the declared output space; the image may be smaller than the codomain.",
    "example": "For T(x,y)=(x,0), the kernel is the vertical axis.",
    "confusion": "Zero-only kernel implies injectivity, not necessarily surjectivity.",
    "lesson": "linear-algebra-transformations",
    "section": "Kernel, image, and information loss",
    "related": ["linear-algebra-transformations-3"]
  },
  {
    "id": "la-term-image",
    "kind": "term",
    "name": "Image of a linear map",
    "aliases": ["Image of a linear map"],
    "linkAliases": [],
    "quick": "The subspace of outputs actually attained by the map.",
    "definition": "The subspace of outputs actually attained by the map.\n\nThe kernel of a linear transformation is the set of inputs sent to zero. The image is the set of outputs it actually reaches. For a matrix rule, these are respectively the null space and column space already studied. The domain specifies permitted inputs, while the codomain specifies the declared output space; the image may be smaller than the codomain.",
    "example": "For T(x,y)=(x,0), the image is the horizontal axis.",
    "confusion": "The declared codomain may be larger than the image.",
    "lesson": "linear-algebra-transformations",
    "section": "Kernel, image, and information loss",
    "related": ["linear-algebra-transformations-3"]
  },
  {
    "id": "la-term-affine",
    "kind": "term",
    "name": "Affine map",
    "aliases": ["Affine map"],
    "linkAliases": [],
    "quick": "A matrix rule plus a fixed offset: Ax+b.",
    "definition": "A matrix rule plus a fixed offset: Ax+b.\n\nA rule Ax+b with a fixed offset vector b is called affine. It is linear precisely when b is zero. Translations are affine and preserve straight lines, but a nonzero translation does not preserve the origin. In everyday language “linear graph” can mean a straight line with an intercept; in linear algebra, the zero-preserving condition is essential.",
    "example": "Translation by (1,2) is affine but not linear.",
    "confusion": "An affine map is linear precisely when its offset is zero.",
    "lesson": "linear-algebra-transformations",
    "section": "Coordinates, composition, and affine rules",
    "related": ["linear-algebra-transformations-4"]
  },
  {
    "id": "la-term-endomorphism",
    "kind": "term",
    "name": "Endomorphism",
    "aliases": ["Endomorphism"],
    "linkAliases": [],
    "quick": "A linear map from a vector space to itself.",
    "definition": "A linear map from a vector space to itself.\n\nThe formula with the same P on both sides applies to an endomorphism, meaning a linear map from a space to itself, when the same new basis is used for input and output. With different spaces or separately chosen bases, use the input basis matrix on the right and the inverse output basis matrix on the left. Do not force a rectangular map into the square change-of-basis formula.",
    "example": "A rotation of the real coordinate plane is an endomorphism.",
    "confusion": "A rectangular map between coordinate spaces of different dimensions is not an endomorphism.",
    "lesson": "linear-algebra-transformations",
    "section": "Coordinates, composition, and affine rules",
    "related": ["linear-algebra-transformations-4"]
  },
  {
    "id": "la-term-residual",
    "kind": "term",
    "name": "Residual",
    "aliases": ["Residual"],
    "linkAliases": [],
    "quick": "The target vector minus its fitted or projected output.",
    "definition": "The target vector minus its fitted or projected output.\n\nThe residual r is the difference b-p, pointing from the approximation to the target. Substitution into its dot product with u gives zero, so the residual is perpendicular to the permitted line. For b=(3,1) and u=(1,0), the projection is (3,0) and the residual (0,1).",
    "example": "Target (3,1) minus projected output (3,0) gives residual (0,1).",
    "confusion": "Residuals describe discrepancies, not coefficient vectors; the sign convention is target minus fit here.",
    "lesson": "linear-algebra-projections",
    "section": "Projecting onto a line",
    "related": ["linear-algebra-projections-1"]
  },
  {
    "id": "la-term-gram-schmidt",
    "kind": "term",
    "name": "Gram–Schmidt procedure",
    "aliases": ["Gram–Schmidt procedure"],
    "linkAliases": [],
    "quick": "Subtract projections onto earlier orthonormal directions, then normalize each nonzero remainder.",
    "definition": "Subtract projections onto earlier orthonormal directions, then normalize each nonzero remainder.\n\nGram–Schmidt is a procedure for turning an independent list into an orthonormal list spanning the same space. Keep the first vector's direction and normalize it. From the next vector, subtract its projections onto all the directions already retained, then normalize the remainder. Independence ensures the remainder is nonzero in exact arithmetic. Dependent input needs separate handling rather than division by zero.",
    "example": "For (1,0),(1,1), retain (1,0), subtract it from the second vector, and retain (0,1).",
    "confusion": "A zero remainder signals dependence and cannot be normalized.",
    "lesson": "linear-algebra-projections",
    "section": "Orthonormal directions",
    "related": ["linear-algebra-projections-2"]
  },
  {
    "id": "la-term-qr",
    "kind": "term",
    "name": "QR factorization",
    "aliases": ["QR factorization"],
    "linkAliases": [],
    "quick": "A factorization A=QR with orthonormal columns in Q and upper-triangular combination coefficients in R.",
    "definition": "A factorization A=QR with orthonormal columns in Q and upper-triangular combination coefficients in R.\n\nThis construction also explains QR factorization: for an independent-column matrix A, put the orthonormal directions in Q and their combination coefficients in an upper-triangular matrix R, so A=QR. Upper triangular means entries below the main diagonal are zero. We name QR here to explain the numerical-method reference later; the practice requires only the small Gram–Schmidt construction just shown.",
    "example": "Columns (1,0),(1,1) give Q=identity and R with rows (1,1),(0,1).",
    "confusion": "Q may be rectangular; its transpose is then not a two-sided inverse.",
    "lesson": "linear-algebra-projections",
    "section": "Orthonormal directions",
    "related": ["linear-algebra-projections-2"]
  },
  {
    "id": "la-term-design-matrix",
    "kind": "term",
    "name": "Design matrix",
    "aliases": ["Design matrix"],
    "linkAliases": [],
    "quick": "The matrix whose rows give the model coefficients for each observation.",
    "definition": "The matrix whose rows give the model coefficients for each observation.\n\nFit predicted values c+dt to observations (t,y)=(0,1),(1,2),(2,2). The design matrix, meaning the matrix of model coefficients, has rows (1,0),(1,1),(1,2). Its first column multiplies the intercept c; its second multiplies the slope d. The normal equations are 3c+3d=5 and 3c+5d=6. Subtraction gives 2d=1, so d=1/2 and c=7/6.",
    "example": "For predictions c+dt at t=0,1,2, the rows are (1,0),(1,1),(1,2).",
    "confusion": "Its columns multiply model coefficients; the observed values belong in the separate target vector.",
    "lesson": "linear-algebra-projections",
    "section": "Least squares and the normal equations",
    "related": ["linear-algebra-projections-3"]
  },
  {
    "id": "la-term-normal-equations",
    "kind": "term",
    "name": "Normal equations",
    "aliases": ["Normal equations"],
    "linkAliases": [],
    "quick": "The equations A transpose A x=A transpose b characterizing least-squares coefficient vectors.",
    "definition": "The equations A transpose A x=A transpose b characterizing least-squares coefficient vectors.\n\nFor a matrix A and target b, a [least-squares](ref:linear-algebra-projections-3) solution chooses x to minimize the squared Euclidean norm of Ax-b. It does not usually satisfy every original equation. It selects the output in the column space nearest to b. The residual must be orthogonal to every column of A, giving $A^T(Ax-b)=0$. Rearranging produces the normal equations, $A^TAx=A^Tb$.",
    "example": "Fitting a constant to 1,2,6 gives 3c=9.",
    "confusion": "Normal means perpendicular; the normal-equation matrix need not be invertible.",
    "lesson": "linear-algebra-projections",
    "section": "Least squares and the normal equations",
    "related": ["linear-algebra-projections-3"]
  },
  {
    "id": "la-term-characteristic-polynomial",
    "kind": "term",
    "name": "Characteristic polynomial",
    "aliases": ["Characteristic polynomial"],
    "linkAliases": [],
    "quick": "The determinant of A minus lambda times identity; its roots are eigenvalues in the chosen number system.",
    "definition": "The determinant of A minus lambda times identity; its roots are eigenvalues in the chosen number system.\n\nThe determinant expression is called the characteristic polynomial. A root's algebraic multiplicity is the number of times its factor repeats in that polynomial. Its geometric multiplicity is the dimension of its eigenspace. A repeated root does not tell you its eigenspace dimension; solving the homogeneous system is still necessary. Here both roots occur once.",
    "example": "For rows (2,1),(0,3), it is (2-lambda)(3-lambda).",
    "confusion": "Finding polynomial roots does not yet find the eigenspaces.",
    "lesson": "linear-algebra-eigenvalues",
    "section": "Finding eigenvalues in two dimensions",
    "related": ["linear-algebra-eigenvalues-2"]
  },
  {
    "id": "la-term-algebraic-multiplicity",
    "kind": "term",
    "name": "Algebraic multiplicity",
    "aliases": ["Algebraic multiplicity"],
    "linkAliases": [],
    "quick": "The number of times an eigenvalue occurs as a root of the characteristic polynomial.",
    "definition": "The number of times an eigenvalue occurs as a root of the characteristic polynomial.\n\nThe determinant expression is called the characteristic polynomial. A root's algebraic multiplicity is the number of times its factor repeats in that polynomial. Its geometric multiplicity is the dimension of its eigenspace. A repeated root does not tell you its eigenspace dimension; solving the homogeneous system is still necessary. Here both roots occur once.",
    "example": "For the identity in two dimensions, (1-lambda) squared gives multiplicity two.",
    "confusion": "Repeated roots do not by themselves guarantee enough independent eigenvectors.",
    "lesson": "linear-algebra-eigenvalues",
    "section": "Finding eigenvalues in two dimensions",
    "related": ["linear-algebra-eigenvalues-2"]
  },
  {
    "id": "la-term-geometric-multiplicity",
    "kind": "term",
    "name": "Geometric multiplicity",
    "aliases": ["Geometric multiplicity"],
    "linkAliases": [],
    "quick": "The dimension of the eigenspace for an eigenvalue.",
    "definition": "The dimension of the eigenspace for an eigenvalue.\n\nThe determinant expression is called the characteristic polynomial. A root's algebraic multiplicity is the number of times its factor repeats in that polynomial. Its geometric multiplicity is the dimension of its eigenspace. A repeated root does not tell you its eigenspace dimension; solving the homogeneous system is still necessary. Here both roots occur once.",
    "example": "For rows (1,1),(0,1), eigenvalue one has a one-dimensional eigenspace.",
    "confusion": "This dimension is not necessarily the repeated-root count.",
    "lesson": "linear-algebra-eigenvalues",
    "section": "Finding eigenvalues in two dimensions",
    "related": ["linear-algebra-eigenvalues-2"]
  },
  {
    "id": "la-term-singular-value",
    "kind": "term",
    "name": "Singular value",
    "aliases": ["Singular value"],
    "linkAliases": [],
    "quick": "A nonnegative diagonal scale factor in an SVD.",
    "definition": "A nonnegative diagonal scale factor in an SVD.\n\nThe diagonal numbers are singular values, usually listed from largest to smallest. The columns of V are right singular vectors in the input space; columns of U are left singular vectors in the output space. The Greek capital Sigma names this matrix here, not a summation operation. We use the full SVD convention in this lesson; software may return smaller versions that omit unused directions.",
    "example": "A diagonal matrix with entries -4 and 2 has singular values four and two.",
    "confusion": "Input and output singular directions can differ; singular values are not generally eigenvalues.",
    "lesson": "linear-algebra-svd",
    "section": "Perpendicular directions and scale factors",
    "related": ["linear-algebra-svd-1"]
  },
  {
    "id": "la-term-orthogonal-matrix",
    "kind": "term",
    "name": "Orthogonal matrix",
    "aliases": ["Orthogonal matrix"],
    "linkAliases": [],
    "quick": "A square matrix with orthonormal columns, whose transpose is its inverse.",
    "definition": "A square matrix with orthonormal columns, whose transpose is its inverse.\n\nFor an m by n real matrix A, a [singular value decomposition](ref:linear-algebra-svd-1) is $A=U\\Sigma V^T$. Here U is an m by m orthogonal matrix, V is an n by n orthogonal matrix, and Sigma is an m by n rectangular diagonal matrix. Orthogonal matrix means a square matrix whose columns form an orthonormal basis; its transpose is its inverse. Sigma has nonnegative numbers on its diagonal and zero elsewhere.",
    "example": "The matrix swapping two coordinates is orthogonal and preserves Euclidean length.",
    "confusion": "An orthonormal-column rectangular matrix is not an orthogonal matrix under this square convention.",
    "lesson": "linear-algebra-svd",
    "section": "Perpendicular directions and scale factors",
    "related": ["linear-algebra-svd-1"]
  },
  {
    "id": "la-term-frobenius",
    "kind": "term",
    "name": "Frobenius norm",
    "aliases": ["Frobenius norm"],
    "linkAliases": [],
    "quick": "The square root of the sum of the squares of every matrix entry.",
    "definition": "The square root of the sum of the squares of every matrix entry.\n\nThe [Frobenius norm](ref:linear-algebra-svd-4) of a matrix is the square root of the sum of the squares of all its entries. A fundamental approximation theorem says that truncated SVD minimizes the Frobenius norm of the difference among matrices of rank at most k. We state this theorem without its proof. Its squared error equals the sum of the squares of the discarded singular values. Tied singular values can make the best approximation nonunique.",
    "example": "A diagonal matrix with entries three and four has Frobenius norm five.",
    "confusion": "The squared Frobenius norm is twenty-five in this example; do not omit the final square root.",
    "lesson": "linear-algebra-svd",
    "section": "Approximation and responsible interpretation",
    "related": ["linear-algebra-svd-4"]
  },
  {
    "id": "la-term-truncated-svd",
    "kind": "term",
    "name": "Truncated SVD",
    "aliases": ["Truncated SVD"],
    "linkAliases": [],
    "quick": "An approximation obtained by retaining the largest chosen singular values and discarding the remaining contributions.",
    "definition": "An approximation obtained by retaining the largest chosen singular values and discarding the remaining contributions.\n\nA rank-k truncated SVD keeps the k largest singular values and replaces the others by zero. Here k is a nonnegative integer no larger than the rank. Rank-k means at most k independent output directions. This simplification can store or process an approximation using fewer directions.",
    "example": "For diagonal entries six, three, one, keeping one direction leaves diagonal six, zero, zero and error square root of ten.",
    "confusion": "The approximation minimizes a stated norm under a rank constraint, not all possible notions of useful information.",
    "lesson": "linear-algebra-svd",
    "section": "Approximation and responsible interpretation",
    "related": ["linear-algebra-svd-4"]
  },
  {
    "id": "la-symbol-sigma-value",
    "kind": "symbol",
    "name": "\\sigma_i",
    "aliases": ["\\sigma_i", "sigma_i", "sigma value"],
    "linkAliases": [],
    "quick": "The ith nonnegative singular value; lowercase sigma names one value, while uppercase Sigma names the matrix.",
    "definition": "The index i labels a diagonal position in the rectangular SVD factor, from one through min(m,n). In descending order, sigma sub one is the largest stretch. The input and output directions may differ.",
    "example": "For diagonal values five and two, sigma sub one is five and sigma sub two is two.",
    "confusion": "A wide matrix may have additional null directions beyond its min(m,n) listed singular values.",
    "lesson": "linear-algebra-svd",
    "section": "Perpendicular directions and scale factors",
    "related": ["linear-algebra-svd-1"]
  },
  {
    "id": "la-symbol-determinant",
    "kind": "symbol",
    "name": "\\det(A)",
    "aliases": ["\\det(A)", "det(A)", "determinant"],
    "linkAliases": [],
    "quick": "The determinant of A; in two dimensions, ad-bc for rows (a,b),(c,d).",
    "definition": "Read “determinant of A.” The parentheses enclose the matrix argument. For two-by-two real matrices a nonzero determinant is equivalent to invertibility, and its magnitude is the parallelogram area formed by the columns.",
    "example": "For rows (2,1),(1,1), the determinant is one.",
    "confusion": "The determinant is a scalar, not a matrix or the reciprocal of a matrix.",
    "lesson": "linear-algebra-bases",
    "section": "Invertible matrices",
    "related": ["linear-algebra-bases-3"]
  },
  {
    "id": "la-symbol-system",
    "kind": "symbol",
    "name": "Ax=b",
    "aliases": ["Ax=b", "Ax=b", "system"],
    "linkAliases": [],
    "quick": "Matrix A times the unknown input vector x equals the required output vector b.",
    "definition": "If A has m rows and n columns, x has n components and b has m. The rows give simultaneous scalar equations; the columns give the building blocks whose coefficients are x.",
    "example": "Rows (1,1),(1,-1) and b=(5,1) have solution x=(3,2).",
    "confusion": "The same x must satisfy all rows; b is not another coefficient column unless the system is explicitly augmented.",
    "lesson": "linear-algebra-systems",
    "section": "Constraints and augmented matrices",
    "related": ["linear-algebra-systems-1"]
  },
  {
    "id": "la-symbol-homogeneous",
    "kind": "symbol",
    "name": "Ax=0",
    "aliases": ["Ax=0", "Ax=0", "homogeneous"],
    "linkAliases": [],
    "quick": "A homogeneous linear system: the output is the zero vector of the required size.",
    "definition": "Read “A times x equals zero.” For an m-by-n A, zero on the right has m components and the unknown x has n. Its solution set is the null space.",
    "example": "For the one-row matrix (1,2), every x=t(-2,1) solves the equation.",
    "confusion": "Zero on the right is a vector here, and a nontrivial solution means x itself is nonzero.",
    "lesson": "linear-algebra-systems",
    "section": "Homogeneous systems and verification",
    "related": ["linear-algebra-systems-4"]
  },
  {
    "id": "la-symbol-change-of-basis",
    "kind": "symbol",
    "name": "P^{-1}AP",
    "aliases": ["P^{-1}AP", "P^{-1}AP", "change of basis"],
    "linkAliases": [],
    "quick": "The matrix of a square linear map in one new basis used for both input and output.",
    "definition": "Read right to left: P converts new coordinates to standard coordinates, A applies the map, and P inverse converts the output back. The columns of invertible P are the ordered new basis vectors in standard coordinates.",
    "example": "For P with columns (1,1),(1,-1) and A diagonal (2,1), the new matrix has rows (3/2,1/2),(1/2,3/2).",
    "confusion": "Separate input and output bases require separate basis matrices; this formula does not directly describe an arbitrary rectangular map.",
    "lesson": "linear-algebra-transformations",
    "section": "Coordinates, composition, and affine rules",
    "related": ["linear-algebra-transformations-4"]
  },
  {
    "id": "la-symbol-orthonormal-columns",
    "kind": "symbol",
    "name": "Q^TQ=I",
    "aliases": ["Q^TQ=I", "Q^TQ=I", "orthonormal columns"],
    "linkAliases": [],
    "quick": "The columns of Q have pairwise dot product zero and individual squared length one.",
    "definition": "The entries of Q transpose Q are the dot products between Q columns. If Q has k columns, I here is the k-by-k identity. Q may be rectangular.",
    "example": "For the column Q=(1,0), Q transpose Q is the one-by-one identity.",
    "confusion": "Q Q transpose need not be identity; it is the projection onto the column space of Q.",
    "lesson": "linear-algebra-projections",
    "section": "Orthonormal directions",
    "related": ["linear-algebra-projections-2"]
  },
  {
    "id": "la-symbol-qr",
    "kind": "symbol",
    "name": "A=QR",
    "aliases": ["A=QR", "A=QR", "qr"],
    "linkAliases": [],
    "quick": "Q stores orthonormal directions and upper-triangular R stores the coefficients rebuilding A’s independent columns.",
    "definition": "Read “A equals Q times R.” With n independent columns in an m-by-n A, reduced Q is m-by-n and R is n-by-n. The Gram–Schmidt construction explains how each new column uses the previously obtained directions and its new remainder.",
    "example": "For columns (1,0),(1,1), Q can be identity and R has rows (1,1),(0,1).",
    "confusion": "The orthonormal-column condition does not make a rectangular Q a square orthogonal matrix.",
    "lesson": "linear-algebra-projections",
    "section": "Orthonormal directions",
    "related": ["linear-algebra-projections-2"]
  }
]
